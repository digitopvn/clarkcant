# Conformance traceability

Evidence table for the scope items and acceptance tests in
[implementation-plan.md](implementation-plan.md). Each status must be read together with its
test and limits; having a test does not prove the current run passed.

**Status vocabulary**

- **PASS** — exercised by a test that runs in this repository. The test name is given.
- **BLOCKED** — cannot be exercised without external infrastructure, credentials or hardware. The condition is named.
- **NOT-IMPLEMENTED** — belongs to a phase that this bootstrap did not build.
- **PARTIAL** (scope items only) — some layers are real and tested while others are absent; the split is stated.

This table is checked by `pnpm invariants`, which fails if any identifier is missing.
A status here is never upgraded without the corresponding test appearing alongside it.

**Status now has a machine-readable source.** The status of each scope item (`V01`–`V18`) and of each
capability claim in source code lives in
[`packages/contracts/src/implementation-status.ts`](../packages/contracts/src/implementation-status.ts);
the `V` rows of this table are the readable copy of the same data. `pnpm invariants` fails when the two
disagree, when an `implemented` entry does not name a real test — and the test name must be in the very file
named, so “the schema exists” does not get through — or when a
`partial`/`blocked`/`not-implemented` entry does not name what is missing. Rows `T01`–`T73` have **no**
corresponding registry entry: they are still checked only by the presence of a `| Tnn |` row in the
table — not by an id string appearing somewhere in the document — and by the test
titles quoted in the table, so a quoted title must exist in the repo — while a row that only names a
spec path is not yet matched against any specific test case. Comments in source only point at the
registry with `@status-ref <capabilityId>`, and do not assert a status themselves.

**External gates remain open.** #2 (a real Google Calendar account), #3 (Computer Use signing), #4 (a real voice
provider) and #5 (NodeLink across two real hosts) remain open and remain linked; no row here is upgraded
because a fixture ran green. The registry carries the issue number and the reason for what is missing for each gate.

The unproven part of widgets is named right at the gap: `interaction.detach` is now genuinely closed —
the dev host has its own detached host window (`clark widget dev`'s `/detached` route) and a lease
read/written through the same `claimLiveOwner`/`releaseLiveOwner` of `@clarkcant/core` that `apps/desktop/src/main.mjs`
also calls ([dev-lease.ts](../packages/widget-cli/src/dev-lease.ts)), proven with real Chromium
([detach-real-browser.e2e.spec.ts](../packages/widget-cli/test/detach-real-browser.e2e.spec.ts)). Exactly
one gap remains: `voiceClickParity` needs a voice session, whose prerequisite is a provider account
(#4) — so `V12` remains `PARTIAL`, with that gap alone.

## Acceptance tests

| ID | Status | Scenario | Evidence |
|---|---|---|---|
| T01 | PASS | A retried user command produces one logical task | storage.spec: "replays the original acknowledgement for a retry" |
| T02 | PASS | A resent delegation does not spawn a duplicate | node-link.spec: "returns the recorded outcome for a resent envelope" |
| T03 | PASS | Replayed/out-of-order delivery is deduplicated and gaps are reported | node-link.spec: "advances the peer cursor"; contracts.spec: "reports a sequence gap" |
| T04 | PASS | A second home authority for one conversation is rejected | storage.spec: "refuses a second home authority" |
| T05 | PASS | A node timeout after an external submit becomes unknown, not a rerun | core.spec: "moves the task to uncertain"; contracts.spec: "never resubmits a confirmed effect" |
| T06 | PASS | A revoked grant stops new delegations | storage.spec: "stops returning it after revocation" |
| T07 | PASS | A to B to C gains no rights without explicit grants | contracts.spec: "refuses re-delegation beyond the permitted depth"; core.spec: "stops a task from being forwarded" |
| T08 | PASS | A private network without application auth is rejected | node-link.spec: "rejects an envelope whose claimed sender is not the authenticated peer" |
| T09 | PASS | A wrong node-qualified resource is rejected before any effect | core.spec: "treats resource locality as a hard constraint" |
| T10 | PASS | An unapproved artifact transfer is refused with a reason | contracts.spec: "refuses an executable content type regardless of declared size" |
| T11 | PASS | An unsupported peer version negotiates a subset or reports unavailable | contracts.spec: version negotiation suite |
| T12 | PASS | Closing the desktop leaves server jobs running | The work is a record on the node, not a subscription held by whoever asked, so the window that asked going away cannot stop it: `apps/runtime/test/background-sessions.spec.ts` — "keeps the work on the node, so it outlives the request that asked for it" — and the route refuses a body it cannot use rather than accepting work it will never run (`apps/runtime/test/background-route.spec.ts`). The budget that bounds the run is the task's own (`maxWallClockMs`). A restart is not silent either: `apps/runtime/test/work-recovery.spec.ts` — "reports an interrupted command once in its conversation and never re-runs it", "re-runs effect-free background work once, in Autonomous mode, within a day". |
| T13 | PASS | An offline home defers approval rather than self-approving | An approval is decided only by the conversation's home authority: the approval carries its task, the task carries its conversation, and the conversation carries the home node, so a node that is not the home refuses and the work waits instead of being approved by whoever happens to hold it. `packages/core/test/core.spec.ts` — "refuses to decide an approval for a conversation whose home is another node" (and the decision stays pending). The unreachable case is the same refusal, which is why this holds without a reachability probe; that probe belongs to P4. |
| T14 | PASS | Two writers on one resource are serialized | storage.spec: "allows only one live lease per resource"; core.spec: "allocates a monotonically increasing epoch" |
| T15 | PASS | A changed Git common ref is caught by a repo lock | worktree.spec.ts: a lock records the shared git directory and the commit at HEAD against real repositories; a commit landing under a running task and a path re-pointed to another repository are both caught |
| T16 | PASS | A dirty working tree is never reset or stashed over | worktree.spec.ts: an uncommitted edit and an untracked file are both refused; the user text is byte-identical afterwards, HEAD has not moved, and git stash list is empty |
| T17 | PASS | A session file with a dead worker is not reported as running | core.spec: "refuses an illegal transition instead of ignoring it" |
| T18 | PASS | An idle worker with failing tests is not success | core.spec: success gating suite |
| T19 | PASS | An ambiguous project produces one clarifying question | core.spec: "asks one clarifying question when a project is ambiguous" |
| T20 | PASS | Two tasks needing one pack join a single install plan | core.spec: "lets two tasks needing the same pack join one plan" |
| T21 | PASS | A changed source/version/digest invalidates prior consent | core.spec: "invalidates consent when the artifact digest moved"; contracts.spec: "invalidates consent when the target node changes" |
| T22 | PASS | A malicious lifecycle script does not run in a credential-rich host | seams.spec: "never forwards a credential-bearing environment variable" |
| T23 | PASS | An invalid OS/arch native extension is blocked, not ready | A package whose manifest does not list this host's OS/arch is refused before anything is downloaded (`PLATFORM_MISMATCH`), and a facet that cannot load is reported as not usable with its reason rather than offered: `packages/core/test/package-sources.spec.ts` — "refuses a package that does not list this platform", "refuses a Windows-only package on a host that is not Windows" — and `packages/core/test/core.spec.ts` — "says why no node can run a capability rather than picking one anyway". The check is at the declared-platform boundary, which is where the block happens; this repository ships no native binary to load. |
| T24 | PASS | A UI-only package update does not restart Pi or voice | contracts.spec: refresh scoping suite; capability-host test |
| T25 | PASS | A repeated reload leaves no stale handler or duplicate listener | pi-adapter.spec: "refuses to subscribe the same listener twice" |
| T26 | PASS | A failed activation leaves the prior generation serving | core.spec: "keeps the previous generation usable after a failed activation" |
| T27 | PASS | A changed original intent is revalidated before resuming | continuation.spec.ts: a changed goal refuses to resume and invalidates every prior approval, while a goal that differs only in whitespace and case resumes |
| T28 | PASS | A native extension asking for global secrets is refused or labelled | secrets.spec.ts: reading every secret is refused outright rather than labelled, while a connection-scoped request is granted only with a stated reason and a label naming the extension, the credential and the reason |
| T29 | PASS | A discovered but unauthenticated tool reports setup required | core.spec: "distinguishes 'needs a connection' from 'not installed'" |
| T30 | PASS | Denied/partial/expired OAuth produces an honest state | seams.spec: "reports partial consent instead of implying full access" |
| T31 | PASS | A callback after a cancelled setup does not activate anything | contracts.spec: pairing invite suite |
| T32 | PASS | Google desktop OAuth uses the system browser | The desktop's only way out is an allowlisted `openExternal`, which hands https to the OS and refuses every other scheme, and no embedded webview is enabled: `apps/desktop/test/security.spec.ts` — "openExternal only opens https" (accepts an https URL; refuses the schemes that turn a link into a code path or a local file read; refuses input that is not a URL) and "does not enable the webview tag or experimental features". The authorization flow itself still needs a registered client; the route it must take does not. |
| T33 | PASS | A loopback callback on a headless server is caught as a mismatch | contracts.spec: checkRedirectReachable |
| T34 | PASS | An API key typed into ordinary chat is redirected to a secure route and redacted | seams.spec: credential vault suite and redaction test |
| T35 | PASS | A mismatched MCP token audience or redirect is rejected | seams.spec: "refuses to forward a token issued for a different resource" |
| T36 | PASS | A cached calendar is labelled with its last update, not as live | seams.spec: "labels cached data as cached rather than live"; e2e asserts the freshness badge in the DOM |
| T37 | PASS | A stale ETag conflict preserves the draft | consent.spec.ts: a write composed against a superseded version is refused, the newer content is untouched, and the caller text survives as a draft |
| T38 | PASS | A calendar create timeout inspects before duplicating | seams.spec: "treats a submit timeout as unknown rather than repeating the create" |
| T39 | PASS | An agent-generated new action binds a discovered capability | consent.spec.ts: a binding is created for a discovered capability and refused with a discover-first code for one that is not, and nothing is persisted when it refuses |
| T40 | PASS | An action naming a nonexistent tool is rejected | contracts.spec: "refuses to bind a capability the registry does not know"; core.spec: same |
| T41 | PASS | A widget cannot forge a host authorization card | seams.spec: "dropped a host card supplied by a non-host origin"; contracts.spec: surface provenance |
| T42 | PASS | A changed account or node rebinds and invalidates old consent | consent.spec.ts: pending approvals naming the previous account or node are expired rather than deleted, decided approvals are left alone, and rebinding to the same target is a no-op |
| T43 | PASS | A double click produces one accepted effect | core.spec: "refuses an invocation whose revision has moved"; storage dedup by invocation id; mini-app-actions.spec: "turns a double click into one effect and one pin" asserts one pin and one recorded invocation from two identical requests; mini-app-ownership.spec: the same invocation id with different input is refused rather than applied twice |
| T44 | PASS | An invalid or oversized widget spec falls back and chat stays usable | seams.spec: "falls back to the text alternative"; e2e/widget.spec: "a widget the client cannot render shows its text alternative instead of nothing" writes a real surface block for a definition no client ships and asserts the fallback text renders rather than a blank card; surface-composition.spec: an oversized bundle is refused rather than truncated; mini-app-compose.spec: a template that cannot compile returns a refusal instead of a half-built surface |
| T45 | PASS | A custom iframe cannot read host storage | seams.spec: "never grants a custom mini-app a same-origin luxury" |
| T46 | PASS | A user-authored note widget builds, previews, approves and persists a draft | widget-lifecycle.spec: a stale autosave is refused, the committed state is untouched and the draft survives; one draft per widget is held by the surface that opened it |
| T47 | PASS | Pinning an inline widget keeps one logical instance | core.spec: "refuses a second live owner for the same instance"; mini-app-ownership.spec: a second claim is refused with the holder named, a claim whose lease expired is recoverable, and an expired legacy claim does not lock the instance; mini-app-actions.spec: the live-owner route refuses the second surface and requires the token to release; e2e/mini-app.spec: a second browser surface renders read-only with a reason instead of taking the instance over; [detached-window](../apps/desktop/test/detached-window.spec.ts): the bootstrap a detached window receives carries the widget composition and cannot be widened with a token, a gateway URL or a conversation id, an intent that tries to carry a credential is refused, and `reviewIpcCall` refuses each window the other's channels; the desktop smoke test asserts the shell bridge reaches none of the detached window's own verbs |
| T48 | PASS | Restoring a pinned media widget does not autoplay or join | widget-lifecycle.spec: restorePinnedInstance returns the stored position with playing false and a stated reason; a second restore is also silent |
| T49 | PASS | Unpinning a note preserves its data | e2e/j1.spec.ts: "pinning and unpinning keeps the widget data" asserts the instance survives the unpin; mini-app-ownership.spec: "saves the view and pins the same instance in one step" asserts the instance, its state and its captured snapshot all survive an unpin, and that a pin points at the logical instance rather than creating a second one |
| T50 | PASS | A disabled pack still shows a safe history snapshot | widget-lifecycle.spec: disabling a package moves its instances offline, keeps the snapshot readable and reports actions unavailable; instances from other packages are untouched; mini-app-actions.spec: the snapshot presentation route answers with the captured bundle and `readOnly: true`, so history renders without any action binding; surface-snapshot.spec: a snapshot whose source was deleted keeps its text alternative and reports a tombstone instead of re-reading live data |
| T51 | PASS | A failed widget state migration recovers the old snapshot | widget-lifecycle.spec: a step that throws leaves the stored document and version at their original values, read back from storage rather than from the returned copy |
| T52 | PASS | An offscreen widget's background polling is rate-limited | consent.spec.ts: a manual subscription never polls, an on-open one only while on screen, and being offscreen cannot shorten the bounded interval |
| T53 | PASS | A stale browser locator re-observes instead of clicking | driver.spec.ts: a missing element reference and a moved target version are both refused against a real Chromium |
| T54 | PASS | A page prompt injection cannot alter rights, consent or secret scope | injection.spec.ts: a page instructing the agent to grant itself capabilities, skip approval, reveal secrets and exfiltrate is refused on every path; a differential case proves the refusal is about approval rather than a blanket refusal |
| T55 | PASS | A browser submit timeout does not trigger a second submit | submit-once.spec.ts: a click that fires its request then stops reporting returns unknown, resending the same action is refused and the server records exactly one submission, and observing lifts the block |
| T56 | PASS | Human login takeover pauses agent input and capture | driver.spec.ts: sensitive input is detected by focus and input is refused while it holds; takeover refuses input too |
| T57 | PASS | A denied or revoked macOS permission reports unavailable without bypass | seams.spec: "keeps capture and input permissions separate on macOS" |
| T58 | PASS | A foreground window or DPI change is validated before input | seams.spec: "revalidates the foreground target before sending input" |
| T59 | PASS | A local emergency stop outranks a remote command via lease fencing | core.spec: "fences out a holder whose epoch is behind"; "records a local emergency stop" |
| T60 | PASS | A headless Linux node makes no native-desktop claim | seams.spec: "reports a Linux host without a display as unable to run a virtual desktop" |
| T61 | PASS | Virtual desktop access without session auth is rejected | An action is admitted only for a session that exists, is running, holds the caller's lease epoch and has a preview the node can actually observe; otherwise it is refused by name (`NO_SUCH_SESSION`, `STALE_LEASE`, `USER_HAS_CONTROL`, `SESSION_STOPPED`, and the preview gate): `packages/core/test/control-sessions.spec.ts` — "tells an unknown session apart from a stopped one", "refuses the action the agent had already planned", "refuses to act on a surface it cannot observe, and says why", "admits an action once the preview is granted, without changing who drives". |
| T62 | PASS | Quick play without provider credentials is clearly a sample | e2e/j1.spec.ts: "a suggestion produces a labelled sample with a real widget" asserts the host-owned label in a real browser |
| T63 | PASS | Skipped or restarted onboarding resumes without reinstalling | onboarding.spec.ts: a completed step is not asked again, checkpoints are scoped per node so one machine setup does not mark another done, and progress is counted against the whole catalogue |
| T64 | PASS | A spoken interruption changes audio only and does not cancel the job | contracts.spec and seams.spec: barge-in routing |
| T65 | PASS | A mid-sentence correction is one task revision, not two effects | seams.spec: "emits one intent per completed utterance" |
| T66 | PASS | Voice and click reach the same widget action state | Built on both sides and unit-tested: one shared `invokeWidgetAction` for a click and a sentence, a resolver with 10 tests (`apps/runtime/test/widget-voice-action.spec.ts`, which also carries the period argument a sentence implies), the `focus` frame carrying only an instance id, and the node's own revision and binding digest rather than a cursor from the page. The browser journey is green and in the suite: `apps/web/e2e/voice-widget-action.spec.ts`, "a spoken action and the same click reach the same state", which drives the same action twice with the state put back in between, plus the refusal journey. Fixed at its cause after measuring it, which the earlier note got wrong: the sentence does resolve and the action does run - the node reported `ok` and revision 7 to 8 - but the page had no handler for the node's `widget-action-result` frame, so the surface kept showing the old period. The spoken sentence must also carry the argument: this action takes a period, and a sentence naming only the action cannot choose one. Not counted: the invocation count, because `action_invocations` has no route, so the claim is what is observable - both paths land on the same state. |
| T67 | PASS | Competing microphone owners are resolved explicitly | seams.spec: "refuses a second exclusive microphone owner and names the holder" |
| T68 | PASS | Mute and end actually stop capture and playback on the real transport | The client is the side that holds the microphone, so it is where mute and end either stop the track or do not: mute disables every capture track and sends `{type:"mute", muted}`; end sends `{type:"end"}`, stops every track and closes the audio context ([voice-transport](../packages/conversation-client/test/voice-transport.spec.ts), three tests). The node's half - the frames arriving and the adapter being told - is covered by [voice-gateway](../apps/runtime/test/voice-gateway.spec.ts), and the same path against a real socket in a real browser by [voice](../apps/web/e2e/voice.spec.ts). |
| T69 | PASS | Backup restore with version drift migrates or blocks, never corrupts silently | storage.spec: consistent backup and restore suite |
| T70 | PASS | Reaching a disk or budget limit stops new work safely | hardening.spec.ts: the ceiling is checked at the door and an over-budget run is refused before it starts; windows reset so a limit is not permanent; usage accumulates per window rather than per run; task-dispatch.spec.ts: "kills a worker and fails the task when its wall-clock budget is exhausted", "refuses to accept a run whose usage exceeded the task's token budget, even with verified evidence"; work-supervisor.spec.ts: "refuses in words when the queue is full, and names what is running", "aborts a run that overruns its deadline and records it as failed rather than stopped" |
| T71 | PASS | Credentials are never copied between nodes implicitly | consent.spec.ts: the owning node is handed a handle and never a value, a different node is refused with a routing instruction, and a revoked connection is refused on its own node |
| T72 | PASS | An unavailable vendor SDK yields a working fallback or an unsupported verdict | Both halves measured. With no SDK on the node the capability is not offered as usable and the preflight names the missing prerequisite: `packages/core/test/core.spec.ts` — "reports a capability whose vendor SDK is unavailable as unsupported, and names the missing prerequisite". With a real account the provider path runs: the opt-in live checks (`CLARKCANT_JEV_LIVE=1`) were run against the provider and are green — calibration picks the right routing turn in 8 of 16, and the live selector suite passes. A fixture is never presented as the integration: the sample path is labelled `freshness: "sample"`. |
| T73 | PASS | A spoken app command and the same click reach the same Settings state | Both journeys are green in `apps/web/e2e/voice-control.spec.ts`: the gear and the sentence land on the same tab, and a **named** tab lands where clicking that tab lands. The named-tab journey was red for a measured reason, now fixed at its cause: PR #25 rebuilt the panel, and it carried two effects on the same trigger, the second of which reset the tab to `experience` whenever the panel opened - overwriting the tab a command had named in the same commit. `openAt` alone did not settle it; one effect that honours `openAt` and still defaults to `experience` does. |

## Scope items

| ID | Status | Feature | What is real and what is not |
|---|---|---|---|
| V01 | PASS | Conversation client | One timeline, one composer, pins and a shared React surface are implemented and verified in a real browser ([browser suite](../apps/web/e2e/)). Files can be attached: the composer uploads them, the stored message carries opaque `att_…` references, the timeline shows them, and the agent reads the content through a host tool with no path parameter ([attachments](../apps/web/e2e/attachments.spec.ts); the prompt itself is proven at the adapter seam by `FakePiAdapter.promptsFor()`). The voice surface and the desktop shell both exist now, with a named bridge for the window and the session ([voice](../apps/web/e2e/voice.spec.ts), [desktop](../apps/desktop/src/main.mjs)). The desktop window's own behaviour is proven by that smoke run on a machine with a display: `electron . --smoke-test` exited 0 with every check `true` and `failed: []`, including the bounds, the twenty-by-fifty floor and always-on-top read back from the real window (Electron 44.3.0, Chrome 152). CI runs it too, in the `desktop smoke (xvfb)` job added with the browser suite, so the claim is checked on every push rather than only on the machine where it was first run. |
| V02 | PASS | Portable runtime | `apps/runtime` boots a node with its own identity and database on macOS and Linux, and the gateway refuses unauthenticated requests. **The Unix-socket transport is now built**: `--socket <path>` makes the node listen on a socket instead of opening a port ([unix-socket.ts](../apps/runtime/src/unix-socket.ts)), with three properties kept and tested — the file is mode `0o600` (a socket created under a wide umask lets every process on the machine connect), a stale socket left by a dead node is cleaned up, and the socket of a **live** node is **not** taken over (it asks it first rather than guessing). **The OCI image has a real definition**: a two-stage [Dockerfile](../Dockerfile) with `pnpm install --frozen-lockfile`, and [.dockerignore](../.dockerignore) excludes `node_modules` and `.env` — the image bakes no credential into a layer. The [container-engine.ts](../apps/runtime/src/container-engine.ts) probe runs the engine's own version command rather than looking for a file on `PATH`, and reports `requires-container-engine` naming both attempts when there is no engine. Test: [portable-runtime.spec.ts](../apps/runtime/test/portable-runtime.spec.ts). **Two boundaries are named:** the five socket tests need POSIX and so are skipped on Windows with a named reason (they run on CI's Linux runner), and **the image is not yet built in CI** — no job has a container engine, so the image definition is real while building it is not proven here. |
| V03 | PASS | Persistent task/session runtime | Task/run/effect have evidence in [core tests](../packages/core/test/); the conductor is wired into the runtime through [services.ts](../apps/runtime/src/services.ts) and has [routing tests](../apps/runtime/test/conductor-routing.spec.ts). The worker host in `apps/worker` is complete and has 17 tests of its own (a capability that is not granted is never registered, evidence is produced from real output, settle is not succeed, the budget stops the run, the file tool does not leave the root). The runtime **really launches the worker** through [worker-process.ts](../apps/runtime/src/worker-process.ts), with a [test against a real process](../apps/runtime/test/worker-process.spec.ts): the brief travels by file, exit code 2 means the worker could not run (a dispatch fault) while exit 0 with `not-verified` is a **result** for the caller to judge, and a revoked capability stays revoked across the process boundary. The node now **loads packs itself through its own container** ([pack-load.ts](../apps/runtime/src/pack-load.ts), `services.loadProjectWorkPack`), and readiness is recorded from the record rather than from intent: `loaded` is separate from `healthy`, a run that stays `not-verified` is a run that proves nothing, a `contradicted` run is treated as worse than silence, `authenticated`/`authorized` are not spoken for by the probe, and a worker that cannot run reports its reason instead of throwing — [pack-load.spec.ts](../apps/runtime/test/pack-load.spec.ts), 9 tests, the last of which runs a real worker through the node's own container and asserts that the sentence "no worker has loaded it" disappears. Boot calls it in the background after listening, and does **not** call it on a fixture node, because `CC_SESSION_FIXTURE` exists so that a scripted node does not spawn workers. **Task dispatch now has something that runs it**: `ConductorDeps.runTask` — declared earlier but never called anywhere — is wired in [runtime-bootstrap.ts](../apps/runtime/src/bootstrap/runtime-bootstrap.ts) to [task-dispatch.ts](../apps/runtime/src/task-dispatch.ts): a bounded worker pool (default 2, the rest queue), a per-capability lease through `acquireLease`/`releaseLease` of `@clarkcant/core` (when busy the task goes to `uncertain` for retry, not `failed`), roots checked to lie within the set of roots this node owns before the worker runs (`containingRoot`; anything out of scope is refused and the worker is never started), a time ceiling and an output ceiling for the child process (`worker-process.ts` now builds its env through `execution-supervisor`'s `buildEnvironment(BUILTIN_PROFILES.build)` instead of inheriting the whole `process.env`, and kills the process if it exceeds the byte ceiling), and the run's evidence goes through `runDispatchedTask` exactly like the old path — success only through verification, an unsettled effect blocks. `POST /stop` now also kills running task workers ([emergency-stop.ts](../apps/runtime/src/application/emergency-stop.ts) adds a `tasks` field). A task's result returns to the conversation through `appendHostReply`, the same mechanism as `startBackgroundWork`, so there is no new UI concept — the task shows up through `activeTaskIds` and the existing block. Test: [task-dispatch.spec.ts](../apps/runtime/test/task-dispatch.spec.ts), 5 tests: success with verified evidence, failure when evidence is not-verified, one test that spawns a **real child process** (no `runWorker` mock) and confirms it honestly reports `not-verified` on its own, refusal of a root outside the owned scope before the worker runs, and `stop` killing a hung worker. **Gap closed:** the worker now registers tools under exactly the two refs the pack actually declares — `read_project_file`/`list_project_files` under `project.file.read@1`, and `write_project_file` (bounded write, `read-after-write` evidence) under `project.code.change@1`, in [apps/worker/src/tools.ts](../apps/worker/src/tools.ts). `task-dispatch.ts` also blocks a non-`read` capability through execution policy (Guarded by default) before the worker is started, like every other effect on the node. `FakePiAdapter` (in `@clarkcant/pi-adapter`) now accepts a `ScriptedTurn` that calls a real tool before answering, and `apps/worker/src/main.ts` reads it via `--script`, so a real child process can be driven deterministically in tests. Test: [task-dispatch-project-work-pack.spec.ts](../apps/runtime/test/task-dispatch-project-work-pack.spec.ts) — spawns a real worker process (no injected `runWorker`), the script calls `write_project_file`, the task settles `succeeded` with `read-after-write` evidence, and the file really is on disk with the right content. |
| V04 | PASS | Trusted node linking | Envelope validation, identity checking, invite single-use and revoke are implemented and tested, and pairing is now exercised between two live hosts: two nodes on two ports with two databases and real HTTP between them. The invitation is single-use (a replay is a 409 conflict rather than a 404, so a stolen invitation is not hidden behind a missing one) and expires; a fingerprint must name the key it is offered with; a peer stays pending and is refused an envelope until a person confirms it on both sides; an envelope whose claimed sender is not the node the token belongs to is refused as SENDER_MISMATCH; and a revoked peer is answered exactly as an unknown token is. All of that is `apps/runtime/test/peers.spec.ts`. The device key is Ed25519, generated locally and kept owner-readable only, and peer tokens are derived as an HMAC of the local token and the peer id, so no credential crosses the wire while pairing and only a sha256 of it sits at rest. The transport is HTTP against the peer's own gateway rather than TLS or WebSocket: the socket transport in `packages/node-link` is still a stub, and a node that cannot be reached over plain HTTP cannot be reached today. |
| V05 | PASS | Remote collaboration | Delivery semantics, dedup and grant narrowing are implemented and tested, and a live two-node delegation is now exercised: two nodes with real HTTP between them, an owner-written grant that travels as a pair.confirm envelope, a delegate naming that grant accepted by the receiver, a replay answered from the inbox with the recorded outcome rather than a second execution, a delegation naming a grant the receiver never accepted refused as DELEGATION_UNKNOWN, and an artifact whose classification the grant does not allow refused by the grant rather than by the transfer. All of that is `apps/runtime/test/peers.spec.ts`. The receiver's acceptance policy is the intersection of the live grants from that sender, which is the contract's own rule that a grant is narrowed and never widened by holding another; a grant that sets no artifact budget grants none. Sequence gaps are reported as SEQUENCE_GAP and the peer cursor advances only contiguously, so a delayed message is still processable instead of being read as a regression. Re-delegation depth is carried in the grant but not enforced beyond it. Artifact byte transfer is no longer missing from the peer path: the receiver pulls the bytes and re-hashes them rather than judging only the offer (see V06). |
| V06 | PASS | Workspace registry | Node-qualified resource references, locality routing and lease serialization are all in place and tested. **Explicit file transfer is now built**: the node holding an artifact serves the bytes via `GET /peers/artifacts/<digest>` only to a confirmed peer (derived token, nothing exchanged), and the receiving node **pulls** the bytes itself ([artifact-transfer.ts](../apps/runtime/src/artifact-transfer.ts)), then **re-hashes** them and stores them only when the digest matches — exactly the contract's wording: a transfer is a request, not a side effect, and the digest is verified after transfer rather than only advertised. Three things are kept: **the declared size is a ceiling** (a header over the ceiling is refused before the body is read, and a body over the ceiling is refused too), **redirects are not followed** (`redirect: "error"`), and **a malformed digest is blocked before it is joined into a path** (`digestPrefix` — exactly where a peer might try `../../etc/passwd`). Test: [artifact-transfer.spec.ts](../apps/runtime/test/artifact-transfer.spec.ts), 12 tests, including a case with **two real nodes over HTTP**: node A holds the blob, node B pulls it with the derived token and the bytes arrive intact. **Not yet:** background intake after the node receives an offer (so the peer's acknowledgement is not held up), and no test across two real machines — the two nodes in the test share one process. |
| V07 | PASS | Capability platform | Registry and readiness have evidence in [core tests](../packages/core/test/). Both MCP transports really run: stdio has a [test against a real server process](../packages/mcp-adapters/test/stdio.spec.ts), and streamable-HTTP has a [test against a real HTTP server](../packages/mcp-adapters/test/streamable-http.spec.ts) — the handshake with the echoed session id (the server refuses any request that forgets the session), a `text/event-stream` reply is read even when a non-message event comes first, a request never hangs because a timeout settles it, redirects are refused rather than followed (the fixture redirects to itself, so a client that followed would succeed, which is how the test tells them apart), a tool with a bad schema is named, and a tool that reports its own error surfaces the server's exact text. Not yet: MCP Apps UI resource hosting, and the tool-call loop at the agent layer. |
| V08 | PARTIAL | Conversational install | Plan creation, join-or-create, digest-bound consent, generation activation and rollback are implemented and tested. Marketplace reuses that path rather than adding an installer: a source resolves to an exact artifact ([package-sources](../packages/core/test/package-sources.spec.ts)) and the install runs through the same lifecycle ([install-from-source](../packages/core/test/install-from-source.spec.ts)), with the branch/range/missing-digest refusals and a rollback that is reported as refused rather than claimed when it comes too late. Directory search reuses that same path rather than adding a second one: an index is read and refused whole when one entry is invalid ([directory-index](../packages/core/test/directory-index.spec.ts)), a result without a digest is dropped because the resolver would refuse it, and the card shows source/version/digest/risk with the install control on the row it acts on ([marketplace-search](../apps/web/e2e/marketplace-search.spec.ts) "every install control belongs to the row it acts on", [package-install](../apps/web/e2e/package-install.spec.ts) "a listing can be installed, and says so"); `publish` prepares the entry with the packed artifact's own digest ([conformance](../packages/widget-cli/test/conformance.spec.ts)). **Quarantine download and isolated build are now built** ([quarantine.ts](../packages/capability-host/src/quarantine.ts)): the artifact is downloaded into its own directory and **hashed before anything looks at it** — a digest mismatch **leaves no file behind**, because an unverified file sitting in quarantine is exactly the file a later step could build; unpacking is blocked at two layers (`tar` refuses absolute paths and `..`, then each entry is walked again — **symlinks included**, because a symlink pointing outside the root is how a later write escapes a root that looks sealed); and the build runs in its own process with `cwd` **inside quarantine** (a root outside quarantine is refused outright), with an environment that is **rebuilt** rather than filtered — filtering is how a variable nobody thought of, such as a provider key added later, leaks into a build script — and no shell. Test: [quarantine.spec.ts](../packages/capability-host/test/quarantine.spec.ts), 11 tests (1 skipped on Windows with a named reason). **Dependency locking is now built** ([dependency-lock.ts](../packages/capability-host/src/dependency-lock.ts)): metadata is read **before any execution step** and each request must be **exactly one exact version** with integrity and `resolvedFrom`, so npm / git / local keep their differences instead of being flattened into a version number; **resolving range→version is not this module's job** — `resolveDependencyClosure` does not read `request.spec`, so `^1.2.0`, `latest` or `1.2.5` are only strings the metadata source decides for itself, and the module only refuses any answer that is not exact (`VERSION_NOT_EXACT`), so a range never gets into the lock; the closure is materialized as an immutable artifact — the file name **is** the digest, so a later resolve cannot replace the bytes the plan consented to — with the digest computed by the repo's existing `payloadDigest`, and `lockRef` / `lockDigest` / `lockCoverage` are bound into both the install plan and `package_generations` (the plan also records the provenance of each pin). Drift is **not** re-resolved and carried on: joining a plan with a different closure is refused naming the dependency, a plan recorded before the node froze build input is named as a legacy plan with the action needed rather than read as drift, `consentStillValid` compares the lock too so old consent expires, and the build reads only the persisted lock — missing artifact ⇒ `LOCK_MISSING`, edited contents ⇒ `LOCK_MUTATED`, a reference pointing outside the lock directory ⇒ refused. A lifecycle script that has not been approved does **not** run and the build does not start. Test: [dependency-lock.spec.ts](../packages/capability-host/test/dependency-lock.spec.ts), 22 tests — "pins a floating range to the exact version its metadata names", "runs a build that reads the recorded version, not the one metadata would resolve now", "stops a build whose metadata now resolves to a different closure, naming the dependency", "stops the build when the contents were edited after they were written", "refuses the same artifact-only lock at the runner that spawns, not only in the pipeline in front of it", "grants execution to nothing that was not approved"; plus [install-from-source.spec.ts](../packages/core/test/install-from-source.spec.ts) "refuses to join an existing plan whose closure is not the one resolved now, naming the dependency" and [package-install-route.spec.ts](../apps/runtime/test/package-install-route.spec.ts) "resolves the closure before installing, and reports what the lock covers". **Still missing, which is why V08 stays PARTIAL:** the `/packages/install` route neither downloads nor builds the artifact, so the lock it records is `artifact-only` — it pins the artifact, provenance and build input, and does **not** pin the package's dependency tree; the route now **reads back** the recorded closure with the same reader the build uses and **states plainly** the admission result of the locked-build path (`buildable: false`, reason `artifact-only`) — [package-install-route.spec.ts](../apps/runtime/test/package-install-route.spec.ts) "reports that a build would refuse the closure, and the runner really does" — but `isolatedLockedBuild` is still **not** called from production, and an `artifact-only` lock is refused outright (`LOCK_INCOMPLETE`) at both `prepareLockedBuild` and the `isolatedLockedBuild` runner rather than treated as pinned. A lockfile proves reproducibility; it does **not** prove native code is safe. **Authority boundary (issue #93, P1):** the `/packages/install` route used to read `grantedCapabilities` straight from the request JSON — a client declaring its own grant. The route **no longer reads that field**; `installPackage` always installs with an empty grant set, and the package still installs/activates normally but none of its capabilities is registered as usable merely because it was installed — because this node has no consent/policy state yet from which to derive a real grant for a public install. Test: [package-install-route.spec.ts](../apps/runtime/test/package-install-route.spec.ts) "does not let a forged grantedCapabilities in the request body become authority", [package-install-local-chain.spec.ts](../apps/runtime/test/package-install-local-chain.spec.ts) — the full local install chain over HTTP (plan → policy → frozen closure → activate → listing) and a separate test that names exactly the remaining boundary: the capabilities of a freshly installed package do **not yet** reach `invocationPreflight`, so they **cannot yet** be used for a task dispatch — this is a real seam for a later consent flow, not a hidden gap. **Git/npm sources and consent are now built (#157):** git is fetched at the pinned commit (scheme allowlist, `--` before every argument, hooks/LFS/submodules disabled, a timeout that kills the whole process group), npm at the exact version with integrity checked **before** extraction, and a dedicated ustar reader refuses symlinks/hardlinks/devices, entries escaping the root, name collisions, a pax `size` that disagrees with the header, and anything over the size limit; the digest is checked **before** the artifact enters the cache, and the files route serves only when there is an active generation matching the digest — [package-fetch.spec.ts](../packages/core/test/package-fetch.spec.ts), [package-files-route.spec.ts](../apps/runtime/test/package-files-route.spec.ts). Grants are now **derived** from the capabilities the manifest declares, through execution policy ([install-consent.spec.ts](../packages/core/test/install-consent.spec.ts)); a capability that needs asking creates a real approval, resolved through `POST /packages/approvals/:id/decision` (this approval kind only, in the same transaction as writing the grant) — [package-install-capability-approval.spec.ts](../apps/runtime/test/package-install-capability-approval.spec.ts); an old generation with a `null` grant is resolved once from the manifest — [package-install-legacy-grant-resolution.spec.ts](../apps/runtime/test/package-install-legacy-grant-resolution.spec.ts); the frame receives grant ∩ requested — [widget-frame.spec.ts](../packages/core/test/widget-frame.spec.ts). **Reviewing pending capabilities now has host-owned UI:** `GET /packages/approvals` lists only questions that can still be answered (not expired, and the digest's generation still active), Settings → Extensions & widgets shows the package, version and capability, and the answer is sent with exactly the digest the row displayed — [package-install-capability-approval.spec.ts](../apps/runtime/test/package-install-capability-approval.spec.ts) "lists each open question with the package it is for and the digest the answer must carry", [capability-approvals](../apps/web/e2e/capability-approvals.spec.ts) "a question is answered with the digest it was shown under, and the outcome takes focus". Chat/voice through `manage_package` **only states** the pending question and where to answer it, and cannot approve: the model is not the user. The frame receives only capabilities that are granted **and** pass `invocationPreflight`; the rest are returned as `unavailableCapabilities` with a reason and the UI says so — [widget-frame.spec.ts](../packages/core/test/widget-frame.spec.ts), [package-lifecycle-route.spec.ts](../apps/runtime/test/package-lifecycle-route.spec.ts) "holds back a granted capability that is not ready, names why, and brokers it once it is". **Uninstall / restore / roll back** go through their own route and keep state and snapshots — [package-lifecycle-route.spec.ts](../apps/runtime/test/package-lifecycle-route.spec.ts), [package-lifecycle](../apps/web/e2e/package-lifecycle.spec.ts). `publish` refuses a version that breaks previously published state or bindings (rule §20) — [version-rules.spec.ts](../packages/widget-cli/test/version-rules.spec.ts). **Not yet proven:** a widget package's granted capabilities are still not registered as an executor for task dispatch (deliberately: a grant is the frame's right to ask the host, not something that runs), and there is no remote registry yet; this row stays PARTIAL with exactly those two names. |
| V09 | PARTIAL | Credential/auth setup | An encrypted vault, redaction, PKCE generation, state comparison, scope verification and endpoint allowlisting are implemented and tested. **The authorization-code exchange and refresh loop are now built and run end-to-end** against a token endpoint the test opens itself on loopback ([token-exchange.spec.ts](../packages/integration-sdk/test/token-exchange.spec.ts), 8 tests): PKCE is checked **for real** (the fixture recomputes `s256(code_verifier)` itself and refuses a verifier that does not match the challenge the authorization request carried), a state mismatch returns **before** anything is sent (the fixture records **no** request — this is a matter of ordering, not of the message), a grant missing scopes returns `partial`, a 200 without an access token is refused, an endpoint outside the allowlist and a plaintext endpoint that is not on this machine are both refused, and a silent endpoint times out instead of holding the connection open. `validateEndpoint` accepts exactly one exception and it is not a loosening: loopback HTTP is allowed because traffic to 127.0.0.1 does not leave the machine, so “credentials must not travel in the clear” is not a statement about it. **Not yet proven:** there is no registered OAuth client and no real account, so the endpoint a live connection would use has never been called — this is the machinery of a token exchange, not a token from any provider. |
| V10 | PARTIAL | Reference integration (Google Calendar) | Scope planning, time normalisation, agenda building, conflict detection, write-outcome classification and freshness labelling are implemented and tested. **The read/write path now runs end-to-end** against a Calendar API the test opens itself on loopback, returning JSON in Google's shape ([calendar-api.spec.ts](../packages/integration-sdk/test/calendar-api.spec.ts), 9 tests): `readAgenda` fetches exactly the range (`timeMin`/`timeMax`/`singleEvents`), the token travels in the **header, not the URL** (the fixture records both), and an event's zone is taken from the event itself rather than guessed from the machine; `writeEvent` sends `summary`/`start`/`end` with `timeZone` and classifies the outcome in the node's vocabulary — `applied` when the API returns an id, `refused` when the API refuses **the caller** (401/403), `failed`/`CONFLICT` when the API refuses **the content** (409), and **`unknown` when no response is heard** — because a request that left the machine and then timed out may have created the event, and calling it a failure is how a retry creates a second one. A failed read **never** carries `freshness: "live"`, and an item that is not an event is skipped instead of being turned into an empty event. `eventsUrl` returns `undefined` for a base URL that does not parse instead of throwing out of a helper whose only job is building a string. **The google-calendar pack now calls this client** and is no longer pure logic: `connectionFromGrant` takes the grant the SDK's own authorization-code exchange produces and **refuses to open a connection just because there is a token** (the scopes must cover the read and the capability probe must have passed), then `readAgendaThroughConnector`/`createEventThroughConnector` call the client here ([connector.spec.ts](../packs/google-calendar/test/connector.spec.ts), 6 tests, run against a loopback token endpoint that checks PKCE and a Google-shaped endpoint). **Not yet proven:** there is no Google account, so the API a live connection would call has never been called. |
| V11 | PASS | Rich built-ins | All 14 definitions in the [catalog](../packs/data-canvas/src/index.ts) are now checked **in full** rather than inferred from the families that have tests: each definition has a family (not `unknown`), has a renderer for the client to draw with — except `canvas.overview@1`, which is the composition's **container** and is checked by `Conversation.tsx` before the leaf lookup because it deliberately has no leaf renderer — has a `semanticDescription` as its text alternative, and no id lingers in the family table that the catalog no longer has ([catalog-coverage.spec.ts](../packages/conversation-client/test/catalog-coverage.spec.ts)). The composition part is proven separately: `missingCompositionFamilies` asserts that the node's real registry covers every required family ([mini-app-data.spec.ts](../apps/runtime/test/mini-app-data.spec.ts)). Browser journeys exist for layout, trend, tables and media ([widget tests](../apps/web/e2e/widget.spec.ts), [composition tests](../apps/web/e2e/mini-app.spec.ts)); **metrics, filter, calendar and cta** have renderers and are checked structurally but have no browser journey yet, because the way to reach them is a suggestion chip. |
| V12 | PARTIAL | Custom widgets | Declarative composition has [browser tests](../apps/web/e2e/mini-app.spec.ts). The isolated-app runtime renderer is now proven: the handshake with nonce + the correct source window and every refusal ([widget-sdk runtime](../packages/widget-sdk/test/runtime.spec.ts), [widget-host session](../packages/widget-host/test/session.spec.ts)), and the conformance suite can run those checks against a real package ([widget-cli conformance](../packages/widget-cli/test/conformance.spec.ts)). What is **not** yet proven: MCP Apps sharing this path, and the dev host's in-page script only runs in a browser; see [widget boundaries](widgets-and-extensions.md). **Checks that need a frame can now be answered from a real frame**: `clark widget test --frames <file>` reads the facts the dev host collected in the page and answers **9 of 10** frame-dependent checks from the very `auditFrame`/`evaluateDetach` functions the dev host runs — so the suite and the host cannot disagree about what a frame shows — instead of guessing that the package will be right: keyboard (only `focus-visible` fails; `no-tab-stops` is a warning, because a widget with nothing interactive is a valid widget), touch target, narrow/compact/expanded (rendering that overflows fails, naming the viewport), loading/readOnly, reduced motion, and **detach**. When facts are missing the check **stays** `requires-dev-host` — a collector that did not ask is no reason to default to good — and an unreadable file exits with code 2 rather than silently falling back. **`detach` is now a real window, not a note about somewhere else that proves it**: `clark widget dev`'s `/detached` route opens a real detached host window of the dev host itself, and its ownership goes through the same `claimLiveOwner`/`releaseLiveOwner` of `@clarkcant/core` — the functions that `apps/runtime`'s live-owner route and `apps/desktop/src/main.mjs` also call — via a genuinely migrated in-memory lease store ([dev-lease.ts](../packages/widget-cli/src/dev-lease.ts)), so no second lease algorithm is rewritten here. The sequence follows `apps/desktop`'s order: the shell releases the `inline` claim **before** the detached window takes the `detached` claim, and closing the detached window hands the lease back to the shell over `BroadcastChannel`. The “does this check pass or fail” decision is the pure function `evaluateDetach` ([dev-shell.ts](../packages/widget-cli/src/dev-shell.ts)), separate from fact collection, split along the same principle `auditFrame` already follows. Proven with real Chromium, not assumed: [detach-real-browser.e2e.spec.ts](../packages/widget-cli/test/detach-real-browser.e2e.spec.ts) opens the real dev host, clicks Detach, waits for a real Chromium popup window to open, confirms that a second `inline` claim is refused while the detached window is open (so `everDoubleOwned` is not an optimistic default), clicks Reattach in that window, and reads `/dev/api/live-owner` back after the window closes to confirm the lease has returned to `inline`. The four decision cases (pass, double-owned, did not move, not handed back) are tested separately in [conformance.spec.ts](../packages/widget-cli/test/conformance.spec.ts). **Exactly one `requires-dev-host` check remains**: `voiceClickParity` needs a voice session, which means it needs a provider account (#4) — not something this phase closes. Test: [conformance.spec.ts](../packages/widget-cli/test/conformance.spec.ts), 28 tests; [dev-lease.spec.ts](../packages/widget-cli/test/dev-lease.spec.ts), 4 tests; [detach-real-browser.e2e.spec.ts](../packages/widget-cli/test/detach-real-browser.e2e.spec.ts), 1 test running real Chromium. |
| V13 | PARTIAL | Pins | Pin persistence and single-live-owner enforcement are verified in a browser. Restore-without-autoplay returns the stored position and does not play, verified in core. **The saved position now reaches the vendor player**: `restorePinnedInstance` returns `positionSeconds`, and `vendorEmbedUrl` carries it into the vendor's own `start` parameter instead of letting the embed reopen from the beginning — before this the pinned surface rendered the player but **ignored the position**, so a pin that “reopens at the right spot” actually replayed from second 0. This function exists so that the URL **cannot** express autoplay: no parameter here can turn it on, and the test asserts that for every position including `NaN`/`Infinity` (a broken position must not become a broken URL) — [media-embed.spec.ts](../packages/conversation-client/test/media-embed.spec.ts), 4 tests. **Not yet proven:** **there is still no vendor player** — what is checked is the URL the browser will load, not the behaviour of the real YouTube player (whether it honours `start` and does not autoplay needs a live vendor). |
| V14 | PARTIAL | Browser Use | Chromium driver evidence is in [driver.spec.ts](../packs/browser-playwright/test/driver.spec.ts); prompt injection and non-repeating submit have [injection.spec.ts](../packs/browser-playwright/test/injection.spec.ts) and [submit-once.spec.ts](../packs/browser-playwright/test/submit-once.spec.ts). **The takeover state exists and is tested**: `preview` is a first-class state with `previewReason`, a desktop session starts as `needs-permission` with a reason rather than pretending it can observe the screen, and `admitAction` refuses an action while it cannot yet observe ([control-sessions.spec.ts](../packages/core/test/control-sessions.spec.ts), [computer-session.spec.ts](../apps/web/e2e/computer-session.spec.ts)). ****The preview source is now real**: `BrowserDriver.capturePreview()` captures the frame as **real bytes** (separate from `observe`, exactly as its own comment says: raw frames never enter the event log, so an observation carries a reference and bytes are only taken when someone is looking at the screen), and [session-preview.ts](../apps/runtime/src/session-preview.ts) stores those bytes in the blob store with two things kept: **the bytes decide the type**, not the claim — a capture that returns JSON or an error page is refused with `NOT_AN_IMAGE` instead of being stored and rendered as if it were the screen — and **the viewport travels with the bytes**, because a preview without its size at capture time cannot be presented honestly. Test: [session-preview.spec.ts](../apps/runtime/test/session-preview.spec.ts), 8 tests. **The card now carries and renders that frame**: `browserSessionCardSchema` has `previewFrame` (digest + viewport + capturedAt) **named separately from `preview`** — because `preview` on the card is already the **state** the node reports (whether the surface can be observed) and the client reads it that way; a field with two meanings is how a card says "available" while showing nothing. The node serves the bytes via `GET /previews/<digest>` (placed **after** the bearer check, because the peer-artifact route in front of it is the unauthenticated path; `cache-control: no-store` because a frame is a picture of someone's screen, and the digest is resolved through the blob store's guard rather than joined into the path by hand), and the client renders it in `ControlSessionCardBlock` through a hook placed in a **separate component** (the card has an early return, so a hook inside it would be a conditionally run hook). The image always has a caption stating when it was captured — a frame is **never** presented as the live screen. **Phase 6 closes this part, and closes it with real bytes**: `captureSessionPreview()` in [session-preview.ts](../apps/runtime/src/session-preview.ts) captures through the driver, stores the bytes in the blob store under the same two existing rules (the bytes decide the type, the viewport travels with the bytes), and **stamps the time at the moment the driver is asked** rather than letting the card take the time itself — a card that stamps itself reports a moment nobody observed. The evidence is in [session-preview-real.spec.ts](../apps/runtime/test/session-preview-real.spec.ts): real Chromium, two real pages over HTTP, **two pages yield two different digests** and both remain servable, the served bytes hash to exactly the digest the card names, and the frame is a complete PNG (with IEND) at exactly the size at capture. The browser journey [browser-takeover.spec.ts](../apps/web/e2e/browser-takeover.spec.ts) checks bytes rather than elements: it asks the node for the frame the card names, re-hashes it against the digest, checks that the image decodes to the captured size, and checks that the timestamp on the caption falls within that very run — a fixed 29-byte PNG passes none of the three. The e2e node's blob store holds a real PNG at 1280x720, and **the digest differs between runs** (one run gave `sha256:b434c67a…`, another gave `sha256:c6c80ea6…`) — exactly the property it must have, because it hashes real bytes rather than a fixture constant. Two real bugs exposed while writing this evidence were fixed: the route sniffed its own bytes with a placeholder type and so served every frame as `application/octet-stream`, and the transport replaced the route's `cache-control: no-store` with `private, max-age=300` (the route now declares that intent through `binary.cache`). **Not yet proven**: no real third-party site has ever been driven behind a login — every capture and every action so far has been on pages served by this repository itself — so a real site's consent and anti-automation boundaries remain untouched, and that remains an open gate rather than something this phase closes with a fixture. Investigation note: changing the browser surface's default to `unavailable` was **wrong** and has been reverted — `preview` means “the node can observe this surface”, not “the user can see it”, and because `admitAction` refuses every action when preview is not `available`, that change would have paralysed every browser session. |
| V15 | PARTIAL | Computer Use | Permission gating, containment labelling, target validation and profile validation are implemented and tested. **The seam for the native binding now exists and the probe returns a named reason**: `probeNativeBinding` returns `requires-signed-bundle` when there is no bundle **or when the signature does not verify** (a signature that does not verify is treated exactly like no bundle — both mean this node has nothing it is willing to run), and `requires-container-engine` when the bundle is fine but the engine does not answer, carrying **the engine probe's own detail** (both the `docker` and `podman` attempts are named, because “no engine” and “an engine that does not respond” need two different fixes). The order in `admitNativeAction` is the main point: a binding that cannot load is refused **before** permission is asked, because reporting “permission not granted” for something that could never run would send the user to a settings screen that does not help and hide the real reason; and `containment` travels with the seam's answer rather than being decided afterwards — it is a property of the loaded binding. Test: [native-binding.spec.ts](../packages/execution-supervisor/test/native-binding.spec.ts), 8 tests. **Not yet proven:** **the two missing resources are still missing** — there is no signed bundle and no container engine, so the real native binding has never run; this row stays PARTIAL with exactly those two names. |
| V16 | PASS | Onboarding/personalisation | Quick play works with no credentials and is labelled as sample in a host-owned card, verified in a browser. The first screen now offers what the node knows the person was doing - unfinished work, something pinned, the session itself, a directory used recently - each chip carrying why it is there, with the four written chips as the fallback when there is nothing (`GET /suggestions`, `apps/web/e2e/suggestions.spec.ts`). Needs-based setup and preference undo are built and tested: setup is planned from the needs the person states, it "plans only the steps those needs require", "covers every need the catalogue claims to serve", reports a step as blocked and names what it waits on, and counts progress against the whole catalogue rather than the steps taken so far; undo "undoes what setup wrote and leaves the user's own changes alone" and "removes a preference that did not exist rather than inventing a value" (`packages/core/test/onboarding.spec.ts`). The product side is the first run on a node that has been told nothing: it "asks for nothing it does not need, and offers setup where the answer is used", and the card leads to the tab that fixes it (`apps/web/e2e/onboarding.spec.ts`). |
| V17 | PARTIAL | Live voice | Transcript assembly, intent routing, media-focus arbitration and mute/end semantics are implemented and tested. Commands reach the same execution path a click takes: nine app intents with a confirmation for quitting (`packages/contracts/src/app-intents.ts`, `packages/core/src/app-intents.ts`), and the window intents that actually move the desktop window (`packages/conversation-client/src/Conversation.tsx`). Parity between a sentence and a click is asserted in `apps/web/e2e/voice-control.spec.ts` and `apps/web/e2e/voice-bar.spec.ts`. **A sentence the deterministic registry does not match now reaches the same tool a typed turn reaches**: `control_app` (`apps/runtime/src/node-tools.ts`) is the main agent's own path to `AppIntent`, audited with `source: "voice"` when the turn it ran in was answering a spoken sentence rather than a typed one (`Turn.channel`, read by the tool through the same getter pattern `onEvent` already used, `apps/runtime/src/model-turn.ts`); the decision it emits is forwarded to the browser over the voice socket's existing `{type: "app-intent"}` frame (`apps/runtime/src/bootstrap/voice-bootstrap.ts`, `apps/runtime/src/voice-session.ts`), so it runs through the one client-side executor, `runAppIntent`, a click already reaches. Proven in a real browser with `apps/web/e2e/voice-agent-control.spec.ts`: a typed sentence the registry declines on length and a scripted spoken one both land the session on the same start screen a click reaches, the spoken one carrying `nav.home`'s own read-back. What is substituted there is the model's judgement to call the tool, not the tool itself — see the suite's own header for the limitation, named rather than glossed. The transport is a node-proxied WebSocket to Gemini Live (`packages/voice-adapters/src/gemini-live.ts`, `apps/runtime/src/voice-session.ts`), not browser-direct WebRTC, and the browser holds no provider credential (`docs/research/adr-001-gemini-live-provider.md`); the live-provider checks remain opt-in and need a provider account. **The live check is now recorded as opt-in and has a probe that returns a named reason**: `probeLiveVoice` returns `requires-live-account` when `GEMINI_API_KEY` is not set and the node's vault holds no credential for the principal — it names **what** it looks for and **never** the value, because this probe runs exactly where the credential may be absent, so it must not be the thing that puts the credential into a log; an empty variable counts as absent (a variable someone cleared is not a credential), and a “yes” answer states only the **source** (`environment` or `vault`) with no field for the value to pass through — [voice-live.spec.ts](../apps/runtime/test/voice-live.spec.ts), 5 tests that run every time. A real session with the provider's service is still **opt-in** and is not tried here. **The real model's decision is now proven (opt-in)**: [voice-live-provider.spec.ts](../apps/web/e2e/voice-live-provider.spec.ts) runs with `CC_LIVE_PROVIDER=1` and `CC_LIVE_PROVIDER_TEST=1`, opens a real voice session, feeds in a sentence the registry does not match through a test-only seam (`/voice-live/utterance`, 404 when the flag is missing) and lets **the main agent's real model** decide on its own to call `control_app`; both sentences (open settings, go back to the start screen) reach exactly the `runAppIntent` result a click produces. Run on 2026-09-23: 2 passed. Not run in CI. **Not yet proven:** the seam feeds a completed sentence straight into the connection's `ask()`, so the speech-recognition half (audio → Gemini Live's `inputTranscription`) is not exercised by this check; this row stays PARTIAL with exactly that name. |
| V18 | PASS | Operations/security | Forward-only migrations (`packages/storage/src/migrate.ts`), verifiable consistent backup, restore compatibility checks, credential redaction and durable dedup are all in place and tested. Failure injection and soak are now **built** rather than left for P10: [storage.spec.ts](../packages/storage/test/storage.spec.ts) has **6 failure-injection cases around backup** (a backup cut off before the manifest is written, a manifest pointing at a database that no longer exists, a manifest that does not parse, a database cut off mid-write, a row count that disagrees with the manifest while the bytes are intact, and a 25-round soak of back-to-back create/verify) plus **one failure-injection case around migration** (a migration that dies midway leaves the schema at exactly the last fully applied version, and the table it managed to create disappears too). Writing this suite found **two real defects**: `verifyBackup` threw `ENOENT` and `database disk image is malformed` instead of reporting them — exactly what its own doc forbids, since the operator is deciding whether to restore — and `openDatabase` leaked a handle when a pragma threw midway, leaving the directory containing it undeletable on Windows. **Not yet:** failure injection at the running-process level (SIGKILL a node mid-write); the current suite injects at the file and migration level. |

## Summary

### Known gap: widget performance (Phase 14)

The three Phase 14 performance items live here rather than in the table, because they have **nothing to attach to yet**:

- **pointer Orb loop outside React state** — in place and tested (`apps/web/e2e/orb.spec.ts`, the canvas is not rebuilt
  when the pointer sweeps across it 20 times).
- **no duplicate live subscription after detach** — in place and tested for some time
  (`core.spec.ts`: "holds the one-owner rule across a detached surface too (V20)"). Detach presents the same
  instance somewhere else, does not create a second instance, and the lease refuses a second owner, naming the surface that holds it.
- **heavy widget lazy mount** and **offscreen suspend** — **in place**: a live surface mounts only when it enters the viewport and
  suspends when it leaves (`apps/web/e2e/mini-app.spec.ts`: "a pinned surface mounts near the viewport and suspends when
  it leaves"), and the isolated frame mounts inside that same surface. The two conditions that used to be missing are now in place: the node serves
  the entry of an installed package (`GET /packages/:id/:version/files/*`, with a nonce-based CSP on each response), and
  a real widget's frame runs from the conversation (`apps/web/e2e/widget-frame.spec.ts`: "a widget package runs in a
  sandboxed frame, and its code is the package's").

The tables above are where each requirement's status is looked up; no scope item is declared
complete. Fixtures prove the application's wiring; they do not replace verification against
the real provider. The UX direction in [DESIGN.md](../DESIGN.md) does not upgrade conformance status
on its own without implementation evidence.
