# Conformance traceability

Bảng bằng chứng cho các scope item và acceptance test trong
[implementation-plan.md](implementation-plan.md). Mỗi trạng thái cần được đọc cùng
test và giới hạn tương ứng; việc có test không chứng minh lần chạy hiện tại đã đạt.

**Status vocabulary**

- **PASS** — exercised by a test that runs in this repository. The test name is given.
- **BLOCKED** — cannot be exercised without external infrastructure, credentials or hardware. The condition is named.
- **NOT-IMPLEMENTED** — belongs to a phase that this bootstrap did not build.
- **PARTIAL** (scope items only) — some layers are real and tested while others are absent; the split is stated.

This table is checked by `pnpm invariants`, which fails if any identifier is missing.
A status here is never upgraded without the corresponding test appearing alongside it.

**Trạng thái nay có nguồn máy đọc được.** Trạng thái của từng scope item (`V01`–`V18`) và của từng
capability claim trong source code nằm trong
[`packages/contracts/src/implementation-status.ts`](../packages/contracts/src/implementation-status.ts);
các dòng `V` của bảng này là bản đọc được của cùng dữ liệu đó. `pnpm invariants` fail khi hai bên
lệch nhau, khi một entry `implemented` không nêu test có thật — và tên test phải có trong chính file
được nêu, nên “schema đã tồn tại” không đi qua được — hoặc khi entry
`partial`/`blocked`/`not-implemented` không nêu điều còn thiếu. Các dòng `T01`–`T73` **không** có
entry tương ứng trong registry: chúng vẫn chỉ được kiểm bằng sự có mặt của id và bằng những test
title được trích trong bảng, nên một title được trích phải tồn tại trong repo — còn một dòng chỉ nêu
đường dẫn spec thì chưa được đối chiếu với ca test cụ thể nào. Comment trong source chỉ trỏ về
registry bằng `@status-ref <capabilityId>`, không tự khẳng định trạng thái.

**External gate vẫn mở.** #2 (tài khoản Google Calendar thật), #3 (Computer Use signing), #4 (voice
provider thật) và #5 (NodeLink hai host thật) vẫn mở và vẫn được link; không dòng nào ở đây được nâng
trạng thái vì một fixture chạy xanh. Registry mang issue number cùng lý do còn thiếu cho từng gate.

Phần chưa chứng minh được của widget được gọi tên ngay tại gap, không mượn trạng thái của một issue:
bộ conformance còn để `detach` chưa kiểm vì sản phẩm chưa có cửa sổ host tách rời, và
`voiceClickParity` vì chưa có phiên voice nào chạy — nên `V12` vẫn là `PARTIAL`.

## Acceptance tests

| ID | Status | Scenario | Evidence |
|---|---|---|---|
| T01 | PASS | A retried user command produces one logical task | storage.spec: "replays the original acknowledgement for a retry" |
| T02 | PASS | A resent delegation does not spawn a duplicate | node-link.spec: "returns the recorded outcome for a resent envelope" |
| T03 | PASS | Replayed/out-of-order delivery is deduplicated and gaps are reported | node-link.spec: "advances the peer cursor"; contracts.spec: "reports a sequence gap" |
| T04 | PASS | A second home authority for one conversation is rejected | storage.spec: "refuses a second home authority" |
| T05 | PASS | A node timeout after an external submit becomes unknown, not a rerun | core.spec: "moves the task to uncertain"; contracts.spec: "never resubmits a confirmed effect" |
| T06 | PASS | A revoked grant stops new delegations | storage.spec: "stops returning it after revocation" |
| T07 | PASS | A to B to C gains no rights without explicit grants | contracts.spec: "refuses re-delegation beyond the permitted depth"; core.spec: "stops a task from being forwarded" |
| T08 | PASS | A private network without application auth is rejected | node-link.spec: "rejects an envelope whose claimed sender is not the authenticated peer" |
| T09 | PASS | A wrong node-qualified resource is rejected before any effect | core.spec: "treats resource locality as a hard constraint" |
| T10 | PASS | An unapproved artifact transfer is refused with a reason | contracts.spec: "refuses an executable content type regardless of declared size" |
| T11 | PASS | An unsupported peer version negotiates a subset or reports unavailable | contracts.spec: version negotiation suite |
| T12 | PASS | Closing the desktop leaves server jobs running | The work is a record on the node, not a subscription held by whoever asked, so the window that asked going away cannot stop it: `apps/runtime/test/background-sessions.spec.ts` — "keeps the work on the node, so it outlives the request that asked for it" — and the route refuses a body it cannot use rather than accepting work it will never run (`apps/runtime/test/background-route.spec.ts`). The budget that bounds the run is the task's own (`maxWallClockMs`). |
| T13 | PASS | An offline home defers approval rather than self-approving | An approval is decided only by the conversation's home authority: the approval carries its task, the task carries its conversation, and the conversation carries the home node, so a node that is not the home refuses and the work waits instead of being approved by whoever happens to hold it. `packages/core/test/core.spec.ts` — "refuses to decide an approval for a conversation whose home is another node" (and the decision stays pending). The unreachable case is the same refusal, which is why this holds without a reachability probe; that probe belongs to P4. |
| T14 | PASS | Two writers on one resource are serialized | storage.spec: "allows only one live lease per resource"; core.spec: "allocates a monotonically increasing epoch" |
| T15 | PASS | A changed Git common ref is caught by a repo lock | worktree.spec.ts: a lock records the shared git directory and the commit at HEAD against real repositories; a commit landing under a running task and a path re-pointed to another repository are both caught |
| T16 | PASS | A dirty working tree is never reset or stashed over | worktree.spec.ts: an uncommitted edit and an untracked file are both refused; the user text is byte-identical afterwards, HEAD has not moved, and git stash list is empty |
| T17 | PASS | A session file with a dead worker is not reported as running | core.spec: "refuses an illegal transition instead of ignoring it" |
| T18 | PASS | An idle worker with failing tests is not success | core.spec: success gating suite |
| T19 | PASS | An ambiguous project produces one clarifying question | core.spec: "asks one clarifying question when a project is ambiguous" |
| T20 | PASS | Two tasks needing one pack join a single install plan | core.spec: "lets two tasks needing the same pack join one plan" |
| T21 | PASS | A changed source/version/digest invalidates prior consent | core.spec: "invalidates consent when the artifact digest moved"; contracts.spec: "invalidates consent when the target node changes" |
| T22 | PASS | A malicious lifecycle script does not run in a credential-rich host | seams.spec: "never forwards a credential-bearing environment variable" |
| T23 | PASS | An invalid OS/arch native extension is blocked, not ready | A package whose manifest does not list this host's OS/arch is refused before anything is downloaded (`PLATFORM_MISMATCH`), and a facet that cannot load is reported as not usable with its reason rather than offered: `packages/core/test/package-sources.spec.ts` — "refuses a package that does not list this platform", "refuses a Windows-only package on a host that is not Windows" — and `packages/core/test/core.spec.ts` — "says why no node can run a capability rather than picking one anyway". The check is at the declared-platform boundary, which is where the block happens; this repository ships no native binary to load. |
| T24 | PASS | A UI-only package update does not restart Pi or voice | contracts.spec: refresh scoping suite; capability-host test |
| T25 | PASS | A repeated reload leaves no stale handler or duplicate listener | pi-adapter.spec: "refuses to subscribe the same listener twice" |
| T26 | PASS | A failed activation leaves the prior generation serving | core.spec: "keeps the previous generation usable after a failed activation" |
| T27 | PASS | A changed original intent is revalidated before resuming | continuation.spec.ts: a changed goal refuses to resume and invalidates every prior approval, while a goal that differs only in whitespace and case resumes |
| T28 | PASS | A native extension asking for global secrets is refused or labelled | secrets.spec.ts: reading every secret is refused outright rather than labelled, while a connection-scoped request is granted only with a stated reason and a label naming the extension, the credential and the reason |
| T29 | PASS | A discovered but unauthenticated tool reports setup required | core.spec: "distinguishes 'needs a connection' from 'not installed'" |
| T30 | PASS | Denied/partial/expired OAuth produces an honest state | seams.spec: "reports partial consent instead of implying full access" |
| T31 | PASS | A callback after a cancelled setup does not activate anything | contracts.spec: pairing invite suite |
| T32 | PASS | Google desktop OAuth uses the system browser | The desktop's only way out is an allowlisted `openExternal`, which hands https to the OS and refuses every other scheme, and no embedded webview is enabled: `apps/desktop/test/security.spec.ts` — "openExternal only opens https" (accepts an https URL; refuses the schemes that turn a link into a code path or a local file read; refuses input that is not a URL) and "does not enable the webview tag or experimental features". The authorization flow itself still needs a registered client; the route it must take does not. |
| T33 | PASS | A loopback callback on a headless server is caught as a mismatch | contracts.spec: checkRedirectReachable |
| T34 | PASS | An API key typed into ordinary chat is redirected to a secure route and redacted | seams.spec: credential vault suite and redaction test |
| T35 | PASS | A mismatched MCP token audience or redirect is rejected | seams.spec: "refuses to forward a token issued for a different resource" |
| T36 | PASS | A cached calendar is labelled with its last update, not as live | seams.spec: "labels cached data as cached rather than live"; e2e asserts the freshness badge in the DOM |
| T37 | PASS | A stale ETag conflict preserves the draft | consent.spec.ts: a write composed against a superseded version is refused, the newer content is untouched, and the caller text survives as a draft |
| T38 | PASS | A calendar create timeout inspects before duplicating | seams.spec: "treats a submit timeout as unknown rather than repeating the create" |
| T39 | PASS | An agent-generated new action binds a discovered capability | consent.spec.ts: a binding is created for a discovered capability and refused with a discover-first code for one that is not, and nothing is persisted when it refuses |
| T40 | PASS | An action naming a nonexistent tool is rejected | contracts.spec: "refuses to bind a capability the registry does not know"; core.spec: same |
| T41 | PASS | A widget cannot forge a host authorization card | seams.spec: "dropped a host card supplied by a non-host origin"; contracts.spec: surface provenance |
| T42 | PASS | A changed account or node rebinds and invalidates old consent | consent.spec.ts: pending approvals naming the previous account or node are expired rather than deleted, decided approvals are left alone, and rebinding to the same target is a no-op |
| T43 | PASS | A double click produces one accepted effect | core.spec: "refuses an invocation whose revision has moved"; storage dedup by invocation id; mini-app-actions.spec: "turns a double click into one effect and one pin" asserts one pin and one recorded invocation from two identical requests; mini-app-ownership.spec: the same invocation id with different input is refused rather than applied twice |
| T44 | PASS | An invalid or oversized widget spec falls back and chat stays usable | seams.spec: "falls back to the text alternative"; e2e/widget.spec: "a widget the client cannot render shows its text alternative instead of nothing" writes a real surface block for a definition no client ships and asserts the fallback text renders rather than a blank card; surface-composition.spec: an oversized bundle is refused rather than truncated; mini-app-compose.spec: a template that cannot compile returns a refusal instead of a half-built surface |
| T45 | PASS | A custom iframe cannot read host storage | seams.spec: "never grants a custom mini-app a same-origin luxury" |
| T46 | PASS | A user-authored note widget builds, previews, approves and persists a draft | widget-lifecycle.spec: a stale autosave is refused, the committed state is untouched and the draft survives; one draft per widget is held by the surface that opened it |
| T47 | PASS | Pinning an inline widget keeps one logical instance | core.spec: "refuses a second live owner for the same instance"; mini-app-ownership.spec: a second claim is refused with the holder named, a claim whose lease expired is recoverable, and an expired legacy claim does not lock the instance; mini-app-actions.spec: the live-owner route refuses the second surface and requires the token to release; e2e/mini-app.spec: a second browser surface renders read-only with a reason instead of taking the instance over; [detached-window](../apps/desktop/test/detached-window.spec.ts): the bootstrap a detached window receives carries the widget composition and cannot be widened with a token, a gateway URL or a conversation id, an intent that tries to carry a credential is refused, and `reviewIpcCall` refuses each window the other's channels; the desktop smoke test asserts the shell bridge reaches none of the detached window's own verbs |
| T48 | PASS | Restoring a pinned media widget does not autoplay or join | widget-lifecycle.spec: restorePinnedInstance returns the stored position with playing false and a stated reason; a second restore is also silent |
| T49 | PASS | Unpinning a note preserves its data | e2e/j1.spec.ts: "pinning and unpinning keeps the widget data" asserts the instance survives the unpin; mini-app-ownership.spec: "saves the view and pins the same instance in one step" asserts the instance, its state and its captured snapshot all survive an unpin, and that a pin points at the logical instance rather than creating a second one |
| T50 | PASS | A disabled pack still shows a safe history snapshot | widget-lifecycle.spec: disabling a package moves its instances offline, keeps the snapshot readable and reports actions unavailable; instances from other packages are untouched; mini-app-actions.spec: the snapshot presentation route answers with the captured bundle and `readOnly: true`, so history renders without any action binding; surface-snapshot.spec: a snapshot whose source was deleted keeps its text alternative and reports a tombstone instead of re-reading live data |
| T51 | PASS | A failed widget state migration recovers the old snapshot | widget-lifecycle.spec: a step that throws leaves the stored document and version at their original values, read back from storage rather than from the returned copy |
| T52 | PASS | An offscreen widget's background polling is rate-limited | consent.spec.ts: a manual subscription never polls, an on-open one only while on screen, and being offscreen cannot shorten the bounded interval |
| T53 | PASS | A stale browser locator re-observes instead of clicking | driver.spec.ts: a missing element reference and a moved target version are both refused against a real Chromium |
| T54 | PASS | A page prompt injection cannot alter rights, consent or secret scope | injection.spec.ts: a page instructing the agent to grant itself capabilities, skip approval, reveal secrets and exfiltrate is refused on every path; a differential case proves the refusal is about approval rather than a blanket refusal |
| T55 | PASS | A browser submit timeout does not trigger a second submit | submit-once.spec.ts: a click that fires its request then stops reporting returns unknown, resending the same action is refused and the server records exactly one submission, and observing lifts the block |
| T56 | PASS | Human login takeover pauses agent input and capture | driver.spec.ts: sensitive input is detected by focus and input is refused while it holds; takeover refuses input too |
| T57 | PASS | A denied or revoked macOS permission reports unavailable without bypass | seams.spec: "keeps capture and input permissions separate on macOS" |
| T58 | PASS | A foreground window or DPI change is validated before input | seams.spec: "revalidates the foreground target before sending input" |
| T59 | PASS | A local emergency stop outranks a remote command via lease fencing | core.spec: "fences out a holder whose epoch is behind"; "records a local emergency stop" |
| T60 | PASS | A headless Linux node makes no native-desktop claim | seams.spec: "reports a Linux host without a display as unable to run a virtual desktop" |
| T61 | PASS | Virtual desktop access without session auth is rejected | An action is admitted only for a session that exists, is running, holds the caller's lease epoch and has a preview the node can actually observe; otherwise it is refused by name (`NO_SUCH_SESSION`, `STALE_LEASE`, `USER_HAS_CONTROL`, `SESSION_STOPPED`, and the preview gate): `packages/core/test/control-sessions.spec.ts` — "tells an unknown session apart from a stopped one", "refuses the action the agent had already planned", "refuses to act on a surface it cannot observe, and says why", "admits an action once the preview is granted, without changing who drives". |
| T62 | PASS | Quick play without provider credentials is clearly a sample | e2e/j1.spec.ts: "a suggestion produces a labelled sample with a real widget" asserts the host-owned label in a real browser |
| T63 | PASS | Skipped or restarted onboarding resumes without reinstalling | onboarding.spec.ts: a completed step is not asked again, checkpoints are scoped per node so one machine setup does not mark another done, and progress is counted against the whole catalogue |
| T64 | PASS | A spoken interruption changes audio only and does not cancel the job | contracts.spec and seams.spec: barge-in routing |
| T65 | PASS | A mid-sentence correction is one task revision, not two effects | seams.spec: "emits one intent per completed utterance" |
| T66 | PASS | Voice and click reach the same widget action state | Built on both sides and unit-tested: one shared `invokeWidgetAction` for a click and a sentence, a resolver with 10 tests (`apps/runtime/test/widget-voice-action.spec.ts`, which also carries the period argument a sentence implies), the `focus` frame carrying only an instance id, and the node's own revision and binding digest rather than a cursor from the page. The browser journey is green and in the suite: `apps/web/e2e/voice-widget-action.spec.ts`, "a spoken action and the same click reach the same state", which drives the same action twice with the state put back in between, plus the refusal journey. Fixed at its cause after measuring it, which the earlier note got wrong: the sentence does resolve and the action does run - the node reported `ok` and revision 7 to 8 - but the page had no handler for the node's `widget-action-result` frame, so the surface kept showing the old period. The spoken sentence must also carry the argument: this action takes a period, and a sentence naming only the action cannot choose one. Not counted: the invocation count, because `action_invocations` has no route, so the claim is what is observable - both paths land on the same state. |
| T67 | PASS | Competing microphone owners are resolved explicitly | seams.spec: "refuses a second exclusive microphone owner and names the holder" |
| T68 | PASS | Mute and end actually stop capture and playback on the real transport | The client is the side that holds the microphone, so it is where mute and end either stop the track or do not: mute disables every capture track and sends `{type:"mute", muted}`; end sends `{type:"end"}`, stops every track and closes the audio context ([voice-transport](../packages/conversation-client/test/voice-transport.spec.ts), three tests). The node's half - the frames arriving and the adapter being told - is covered by [voice-gateway](../apps/runtime/test/voice-gateway.spec.ts), and the same path against a real socket in a real browser by [voice](../apps/web/e2e/voice.spec.ts). |
| T69 | PASS | Backup restore with version drift migrates or blocks, never corrupts silently | storage.spec: consistent backup and restore suite |
| T70 | PASS | Reaching a disk or budget limit stops new work safely | hardening.spec.ts: the ceiling is checked at the door and an over-budget run is refused before it starts; windows reset so a limit is not permanent; usage accumulates per window rather than per run |
| T71 | PASS | Credentials are never copied between nodes implicitly | consent.spec.ts: the owning node is handed a handle and never a value, a different node is refused with a routing instruction, and a revoked connection is refused on its own node |
| T72 | PASS | An unavailable vendor SDK yields a working fallback or an unsupported verdict | Both halves measured. With no SDK on the node the capability is not offered as usable and the preflight names the missing prerequisite: `packages/core/test/core.spec.ts` — "reports a capability whose vendor SDK is unavailable as unsupported, and names the missing prerequisite". With a real account the provider path runs: the opt-in live checks (`CLARKCANT_JEV_LIVE=1`) were run against the provider and are green — calibration picks the right routing turn in 8 of 16, and the live selector suite passes. A fixture is never presented as the integration: the sample path is labelled `freshness: "sample"`. |
| T73 | PASS | A spoken app command and the same click reach the same Settings state | Both journeys are green in `apps/web/e2e/voice-control.spec.ts`: the gear and the sentence land on the same tab, and a **named** tab lands where clicking that tab lands. The named-tab journey was red for a measured reason, now fixed at its cause: PR #25 rebuilt the panel, and it carried two effects on the same trigger, the second of which reset the tab to `experience` whenever the panel opened - overwriting the tab a command had named in the same commit. `openAt` alone did not settle it; one effect that honours `openAt` and still defaults to `experience` does. |

## Scope items

| ID | Status | Feature | What is real and what is not |
|---|---|---|---|
| V01 | PASS | Conversation client | One timeline, one composer, pins and a shared React surface are implemented and verified in a real browser ([browser suite](../apps/web/e2e/)). Files can be attached: the composer uploads them, the stored message carries opaque `att_…` references, the timeline shows them, and the agent reads the content through a host tool with no path parameter ([attachments](../apps/web/e2e/attachments.spec.ts); the prompt itself is proven at the adapter seam by `FakePiAdapter.promptsFor()`). The voice surface and the desktop shell both exist now, with a named bridge for the window and the session ([voice](../apps/web/e2e/voice.spec.ts), [desktop](../apps/desktop/src/main.mjs)). The desktop window's own behaviour is proven by that smoke run on a machine with a display: `electron . --smoke-test` exited 0 with every check `true` and `failed: []`, including the bounds, the twenty-by-fifty floor and always-on-top read back from the real window (Electron 44.3.0, Chrome 152). CI runs it too, in the `desktop smoke (xvfb)` job added with the browser suite, so the claim is checked on every push rather than only on the machine where it was first run. |
| V02 | PASS | Portable runtime | `apps/runtime` boots a node with its own identity and database on macOS và Linux, và gateway từ chối request không xác thực. **Transport Unix-socket nay đã được xây**: `--socket <path>` cho node listen trên socket thay vì mở port ([unix-socket.ts](../apps/runtime/src/unix-socket.ts)), với ba tính chất được giữ và được test — file ở mode `0o600` (socket tạo bằng umask rộng thì mọi tiến trình trên máy kết nối được), socket cũ do một node chết để lại thì được dọn, và socket của một node **đang sống** thì **không** bị lấy đi (hỏi nó trước, không đoán). **Image OCI có định nghĩa thật**: [Dockerfile](../Dockerfile) hai stage với `pnpm install --frozen-lockfile`, và [.dockerignore](../.dockerignore) loại `node_modules` cùng `.env` — image không nướng credential vào layer. Probe [container-engine.ts](../apps/runtime/src/container-engine.ts) chạy chính lệnh version của engine thay vì tìm file trên `PATH`, và báo `requires-container-engine` kèm tên cả hai lần thử khi không có engine. Test: [portable-runtime.spec.ts](../apps/runtime/test/portable-runtime.spec.ts). **Hai ranh giới được nêu tên:** năm test socket cần POSIX nên bị skip trên Windows với lý do được nêu tên (chúng chạy trên runner Linux của CI), và **image chưa được build trong CI** — không job nào có container engine, nên định nghĩa image là thật còn việc build nó chưa được chứng minh ở đây. |
| V03 | PASS | Persistent task/session runtime | Task/run/effect có bằng chứng trong [core tests](../packages/core/test/); conductor đã nối với runtime qua [services.ts](../apps/runtime/src/services.ts) và có [routing tests](../apps/runtime/test/conductor-routing.spec.ts). Worker host trong `apps/worker` đã hoàn chỉnh và có 17 test riêng (capability không được cấp thì không bao giờ được đăng ký, evidence sinh từ output thật, settle không phải succeed, budget dừng run, file tool không ra khỏi root). Runtime **thật sự khởi chạy worker** qua [worker-process.ts](../apps/runtime/src/worker-process.ts), có [test với tiến trình thật](../apps/runtime/test/worker-process.spec.ts): brief đi bằng file, exit code 2 là worker không chạy được (lỗi của dispatch) còn exit 0 với `not-verified` là **kết quả** để caller tự phán xử, và capability bị thu hồi vẫn bị thu hồi qua ranh giới tiến trình. Node nay **tự nạp pack qua container của chính nó** ([pack-load.ts](../apps/runtime/src/pack-load.ts), `services.loadProjectWorkPack`), và readiness được ghi từ record chứ không từ ý định: `loaded` tách khỏi `healthy`, một run `not-verified` ở lại là run không chứng minh gì, một run `contradicted` bị coi là tệ hơn im lặng, `authenticated`/`authorized` không bị probe nói thay, và một worker không chạy được thì báo lý do thay vì ném lỗi — [pack-load.spec.ts](../apps/runtime/test/pack-load.spec.ts), 9 test, trong đó test cuối chạy worker thật qua chính container của node và khẳng định câu "no worker has loaded it" biến mất. Boot gọi nó trong nền sau khi listen, và **không** gọi trên node fixture, vì lý do `CC_SESSION_FIXTURE` tồn tại là để node scripted không spawn worker. **Chưa có:** hàng đợi hay worker pool nhiều worker — probe chạy một worker một lần, nên "nhiều task song song" chưa được xây và không thuộc claim của dòng này. |
| V04 | PASS | Trusted node linking | Envelope validation, identity checking, invite single-use and revoke are implemented and tested, and pairing is now exercised between two live hosts: two nodes on two ports with two databases and real HTTP between them. The invitation is single-use (a replay is a 409 conflict rather than a 404, so a stolen invitation is not hidden behind a missing one) and expires; a fingerprint must name the key it is offered with; a peer stays pending and is refused an envelope until a person confirms it on both sides; an envelope whose claimed sender is not the node the token belongs to is refused as SENDER_MISMATCH; and a revoked peer is answered exactly as an unknown token is. All of that is `apps/runtime/test/peers.spec.ts`. The device key is Ed25519, generated locally and kept owner-readable only, and peer tokens are derived as an HMAC of the local token and the peer id, so no credential crosses the wire while pairing and only a sha256 of it sits at rest. The transport is HTTP against the peer's own gateway rather than TLS or WebSocket: the socket transport in `packages/node-link` is still a stub, and a node that cannot be reached over plain HTTP cannot be reached today. |
| V05 | PASS | Remote collaboration | Delivery semantics, dedup and grant narrowing are implemented and tested, and a live two-node delegation is now exercised: two nodes with real HTTP between them, an owner-written grant that travels as a pair.confirm envelope, a delegate naming that grant accepted by the receiver, a replay answered from the inbox with the recorded outcome rather than a second execution, a delegation naming a grant the receiver never accepted refused as DELEGATION_UNKNOWN, and an artifact whose classification the grant does not allow refused by the grant rather than by the transfer. All of that is `apps/runtime/test/peers.spec.ts`. The receiver's acceptance policy is the intersection of the live grants from that sender, which is the contract's own rule that a grant is narrowed and never widened by holding another; a grant that sets no artifact budget grants none. Sequence gaps are reported as SEQUENCE_GAP and the peer cursor advances only contiguously, so a delayed message is still processable instead of being read as a regression. Re-delegation depth is carried in the grant but not enforced beyond it. Artifact byte transfer is no longer missing from the peer path: the receiver pulls the bytes and re-hashes them rather than judging only the offer (xem V06). |
| V06 | PASS | Workspace registry | Node-qualified resource references, locality routing và lease serialization đều đã có và đã test. **Truyền file tường minh nay đã được xây**: node giữ artifact phục vụ byte qua `GET /peers/artifacts/<digest>` chỉ cho peer đã xác nhận (token suy dẫn, không trao đổi), và node nhận **tự kéo** byte về ([artifact-transfer.ts](../apps/runtime/src/artifact-transfer.ts)) rồi **băm lại** và chỉ lưu khi digest khớp — đúng câu của contract: transfer là request chứ không phải hệ quả, và digest được verify sau khi truyền chứ không chỉ được quảng cáo. Ba điều được giữ: **kích thước khai báo là trần** (header vượt trần bị từ chối trước khi đọc body, và body vượt trần cũng bị từ chối), **redirect không được đi theo** (`redirect: "error"`), và **digest sai hình dạng bị chặn trước khi ghép vào đường dẫn** (`digestPrefix` — đúng chỗ một peer có thể thử `../../etc/passwd`). Test: [artifact-transfer.spec.ts](../apps/runtime/test/artifact-transfer.spec.ts), 12 test, trong đó có một ca **hai node thật qua HTTP**: node A giữ blob, node B kéo về bằng token suy dẫn và byte đến nơi nguyên vẹn. **Chưa có:** intake chạy nền sau khi node nhận offer (để không giữ acknowledgement của peer lại), và chưa có test xuyên hai máy thật — hai node trong test cùng một tiến trình. |
| V07 | PASS | Capability platform | Registry và readiness có bằng chứng trong [core tests](../packages/core/test/). Cả hai transport MCP đều chạy thật: stdio có [test với tiến trình server thật](../packages/mcp-adapters/test/stdio.spec.ts), và streamable-HTTP có [test với server HTTP thật](../packages/mcp-adapters/test/streamable-http.spec.ts) — handshake cùng session id được echo (server từ chối request nào quên session), reply dạng `text/event-stream` được đọc kể cả khi có event không phải message đứng trước, request không bao giờ treo vì timeout settle nó, redirect bị từ chối chứ không đi theo (fixture redirect về chính nó nên client đi theo sẽ thành công, nhờ vậy test phân biệt được), tool sai schema thì được nêu tên, và tool tự báo lỗi thì lộ ra nguyên văn của server. Chưa có: MCP Apps UI resource hosting, và tool-call loop ở tầng agent. |
| V08 | PARTIAL | Conversational install | Plan creation, join-or-create, digest-bound consent, generation activation and rollback are implemented and tested. Marketplace reuses that path rather than adding an installer: a source resolves to an exact artifact ([package-sources](../packages/core/test/package-sources.spec.ts)) and the install runs through the same lifecycle ([install-from-source](../packages/core/test/install-from-source.spec.ts)), with the branch/range/missing-digest refusals and a rollback that is reported as refused rather than claimed when it comes too late. Directory search reuses that same path rather than adding a second one: an index is read and refused whole when one entry is invalid ([directory-index](../packages/core/test/directory-index.spec.ts)), a result without a digest is dropped because the resolver would refuse it, and the card shows source/version/digest/risk with the install control on the row it acts on ([marketplace-search](../apps/web/e2e/marketplace-search.spec.ts) "every install control belongs to the row it acts on", [package-install](../apps/web/e2e/package-install.spec.ts) "a listing can be installed, and says so"); `publish` prepares the entry with the packed artifact's own digest ([conformance](../packages/widget-cli/test/conformance.spec.ts)). **Quarantine download và isolated build nay đã được xây** ([quarantine.ts](../packages/capability-host/src/quarantine.ts)): artifact được tải vào thư mục riêng và **băm trước khi bất cứ thứ gì được nhìn vào nó** — digest không khớp thì **không để lại file nào**, vì một file chưa xác minh nằm trong quarantine chính là file mà bước sau có thể build; unpack bị chặn hai lớp (`tar` từ chối đường dẫn tuyệt đối và `..`, rồi từng entry được duyệt lại — **kể cả symlink**, vì symlink trỏ ra ngoài root là cách một lần ghi sau đó thoát khỏi root trông như đã kín); và build chạy trong tiến trình riêng với `cwd` **bên trong quarantine** (root ngoài quarantine bị từ chối thẳng), environment được **dựng lại** thay vì lọc — lọc là cách một biến không ai nghĩ tới, như một provider key thêm sau này, lọt vào build script — và không có shell. Test: [quarantine.spec.ts](../packages/capability-host/test/quarantine.spec.ts), 11 test (1 skip trên Windows với lý do được nêu tên). **Dependency locking nay đã được xây** ([dependency-lock.ts](../packages/capability-host/src/dependency-lock.ts)): metadata được đọc **trước mọi bước thực thi** và mỗi request phải là **đúng một version chính xác** kèm integrity và `resolvedFrom`, nên npm / git / local giữ nguyên khác biệt thay vì bị làm phẳng thành một số version; **việc resolve range→version không thuộc module này** — `resolveDependencyClosure` không đọc `request.spec`, nên `^1.2.0`, `latest` hay `1.2.5` đều chỉ là chuỗi mà metadata source tự quyết định, và module chỉ từ chối bất cứ câu trả lời nào chưa chính xác (`VERSION_NOT_EXACT`), nhờ đó một range không bao giờ vào được lock; closure được materialize thành artifact bất biến — tên file **chính là** digest, nên lần resolve sau không thể thay thế bytes mà plan đã consent — với digest tính bằng `payloadDigest` sẵn có của repo, và `lockRef` / `lockDigest` / `lockCoverage` được bind vào install plan lẫn `package_generations` (plan còn ghi provenance của từng pin). Drift **không** được resolve lại rồi chạy tiếp: join một plan có closure khác bị từ chối kèm tên dependency, một plan ghi trước khi node freeze build input được nêu đúng là legacy plan kèm việc cần làm chứ không bị đọc thành drift, `consentStillValid` so cả lock nên consent cũ hết hiệu lực, và build chỉ đọc lock đã persist — thiếu artifact ⇒ `LOCK_MISSING`, nội dung bị sửa ⇒ `LOCK_MUTATED`, reference trỏ ra ngoài thư mục lock ⇒ bị từ chối. Lifecycle script chưa được approve thì **không** chạy và build không bắt đầu. Test: [dependency-lock.spec.ts](../packages/capability-host/test/dependency-lock.spec.ts), 22 test — "pins a floating range to the exact version its metadata names", "runs a build that reads the recorded version, not the one metadata would resolve now", "stops a build whose metadata now resolves to a different closure, naming the dependency", "stops the build when the contents were edited after they were written", "refuses the same artifact-only lock at the runner that spawns, not only in the pipeline in front of it", "grants execution to nothing that was not approved"; thêm [install-from-source.spec.ts](../packages/core/test/install-from-source.spec.ts) "refuses to join an existing plan whose closure is not the one resolved now, naming the dependency" và [package-install-route.spec.ts](../apps/runtime/test/package-install-route.spec.ts) "resolves the closure before installing, and reports what the lock covers". **Còn thiếu, vì vậy V08 vẫn PARTIAL:** route `/packages/install` không tải và không build artifact, nên lock nó ghi là `artifact-only` — pin artifact, provenance và build input, **không** pin cây phụ thuộc của package; đường build thật (`isolatedLockedBuild`, đã có test) chưa được nối vào runtime install route, và một lock `artifact-only` bị từ chối thẳng (`LOCK_INCOMPLETE`) ở cả `prepareLockedBuild` lẫn runner `isolatedLockedBuild` chứ không được coi là đã pin. Lockfile chứng minh tính tái lập, **không** chứng minh native code an toàn. |
| V09 | PARTIAL | Credential/auth setup | An encrypted vault, redaction, PKCE generation, state comparison, scope verification and endpoint allowlisting are implemented and tested. **Authorization-code exchange và refresh loop nay đã được xây và chạy end-to-end** với một token endpoint do test tự mở trên loopback ([token-exchange.spec.ts](../packages/integration-sdk/test/token-exchange.spec.ts), 8 test): PKCE được kiểm **thật** (fixture tự tính lại `s256(code_verifier)` và từ chối verifier không khớp challenge mà authorization request đã mang), state mismatch trả về **trước khi** gửi bất cứ thứ gì (fixture ghi nhận **không** request nào — đây là chuyện thứ tự, không phải chuyện thông báo), grant thiếu scope trả về `partial`, một 200 không có access token bị từ chối, endpoint ngoài allowlist và endpoint plaintext không nằm trên máy này đều bị từ chối, và endpoint im lặng thì timeout thay vì giữ kết nối. `validateEndpoint` nhận đúng một ngoại lệ và nó không phải nới lỏng: HTTP loopback được cho phép vì traffic tới 127.0.0.1 không rời khỏi máy, nên "credential không được đi trong suốt" không phải câu nói về nó. **Chưa được chứng minh:** không có OAuth client đã đăng ký và không có tài khoản thật, nên endpoint mà một kết nối live sẽ dùng chưa từng được gọi — đây là bộ máy của một token exchange, không phải token từ provider nào. |
| V10 | PARTIAL | Reference integration (Google Calendar) | Scope planning, time normalisation, agenda building, conflict detection, write-outcome classification and freshness labelling are implemented and tested. **Đường đọc/ghi nay đã chạy end-to-end** với một Calendar API do test tự mở trên loopback, trả JSON đúng dạng Google ([calendar-api.spec.ts](../packages/integration-sdk/test/calendar-api.spec.ts), 9 test): `readAgenda` lấy đúng range (`timeMin`/`timeMax`/`singleEvents`), token đi trong **header chứ không trong URL** (fixture ghi nhận cả hai), zone của event lấy từ chính event chứ không đoán theo máy; `writeEvent` gửi `summary`/`start`/`end` kèm `timeZone` và phân loại outcome theo vocabulary của node — `applied` khi API trả về id, `refused` khi API từ chối **người gọi** (401/403), `failed`/`CONFLICT` khi API từ chối **nội dung** (409), và **`unknown` khi không nghe được phản hồi** — vì một request đã rời máy rồi mới timeout có thể đã tạo event, và gọi nó là thất bại là cách để retry tạo ra cái thứ hai. Một lần đọc thất bại **không bao giờ** mang `freshness: "live"`, và một mục không phải event bị bỏ qua thay vì được biến thành event rỗng. `eventsUrl` trả `undefined` cho base URL không parse được thay vì ném ra khỏi một helper chỉ có việc dựng chuỗi. **Chưa được chứng minh:** không có tài khoản Google, nên API mà một kết nối live sẽ gọi chưa từng được gọi. |
| V11 | PASS | Rich built-ins | Cả 14 definition của [catalog](../packs/data-canvas/src/index.ts) nay được kiểm **toàn bộ** chứ không suy ra từ family có test: mỗi definition có family (không phải `unknown`), có renderer để client vẽ — trừ `canvas.overview@1`, đó là **container** của composition và được `Conversation.tsx` kiểm trước leaf lookup vì nó cố ý không có leaf renderer — có `semanticDescription` làm text alternative, và không id nào còn sót trong bảng family mà catalog không còn ([catalog-coverage.spec.ts](../packages/conversation-client/test/catalog-coverage.spec.ts)). Phần composition được chứng minh riêng: `missingCompositionFamilies` khẳng định registry thật của node phủ hết family bắt buộc ([mini-app-data.spec.ts](../apps/runtime/test/mini-app-data.spec.ts)). Journey trình duyệt hiện có cho layout, trend, tables và media ([widget tests](../apps/web/e2e/widget.spec.ts), [composition tests](../apps/web/e2e/mini-app.spec.ts)); **metrics, filter, calendar và cta** có renderer và được kiểm cấu trúc nhưng chưa có journey trình duyệt, vì đường tới chúng là chip gợi ý. |
| V12 | PARTIAL | Custom widgets | Declarative composition có [browser tests](../apps/web/e2e/mini-app.spec.ts). Renderer runtime cho isolated-app giờ đã được chứng minh: handshake với nonce + đúng source window và mọi từ chối ([widget-sdk runtime](../packages/widget-sdk/test/runtime.spec.ts), [widget-host session](../packages/widget-host/test/session.spec.ts)), và bộ conformance chạy được các check đó trên một package thật ([widget-cli conformance](../packages/widget-cli/test/conformance.spec.ts)). Phần **chưa** được chứng minh: MCP Apps dùng chung đường này, và script trong trang của dev host chỉ chạy được trong browser; xem [ranh giới widget](widgets-and-extensions.md). **Các check cần frame nay trả lời được từ một frame thật**: `clark widget test --frames <file>` đọc facts mà dev host thu trong trang và trả lời **8 trong 10** check cần frame từ chính hàm `auditFrame` mà dev host chạy — nên suite và host không thể bất đồng về những gì một frame cho thấy — thay vì đoán rằng package sẽ đúng: keyboard (chỉ `focus-visible` là fail; `no-tab-stops` là warning, vì widget không có gì tương tác là widget hợp lệ), touch target, narrow/compact/expanded (render mà tràn thì fail, kèm viewport), loading/readOnly, và reduced motion. Facts thiếu thì check **ở lại** `requires-dev-host` — một collector không hỏi thì không phải lý do để mặc định là tốt — và một file không đọc được thì thoát code 2 chứ không âm thầm quay về. **Hai check vẫn `requires-dev-host`** vì lý do không thuộc harness: `detach` cần cửa sổ host tách rời mà sản phẩm chưa có, và `voiceClickParity` cần một phiên voice. Test: [conformance.spec.ts](../packages/widget-cli/test/conformance.spec.ts), 24 test. |
| V13 | PARTIAL | Pins | Pin persistence and single-live-owner enforcement are verified in a browser. Restore-without-autoplay returns the stored position and does not play, verified in core. **Vị trí đã lưu nay tới được player vendor**: `restorePinnedInstance` trả `positionSeconds`, và `vendorEmbedUrl` mang nó vào chính tham số `start` của vendor thay vì để embed mở lại từ đầu — trước đó surface được ghim render player nhưng **bỏ qua vị trí**, nên một pin "mở lại đúng chỗ" thực ra phát lại từ giây 0. Hàm này tồn tại để URL **không thể** diễn đạt autoplay: không có tham số nào ở đây bật được nó, và test khẳng định điều đó với mọi vị trí kể cả `NaN`/`Infinity` (một vị trí hỏng không được biến thành URL hỏng) — [media-embed.spec.ts](../packages/conversation-client/test/media-embed.spec.ts), 4 test. **Chưa được chứng minh:** **player vendor vẫn chưa có** — thứ được kiểm là URL mà trình duyệt sẽ tải, không phải hành vi của player YouTube thật (nó có tôn trọng `start` và không tự phát hay không cần một vendor live). |
| V14 | PARTIAL | Browser Use | Bằng chứng driver Chromium nằm trong [driver.spec.ts](../packs/browser-playwright/test/driver.spec.ts); prompt injection và submit không lặp có [injection.spec.ts](../packs/browser-playwright/test/injection.spec.ts) và [submit-once.spec.ts](../packs/browser-playwright/test/submit-once.spec.ts). **Trạng thái takeover thì đã có và đã test**: `preview` là state hạng nhất cùng `previewReason`, một phiên desktop khởi đầu bằng `needs-permission` kèm lý do chứ không giả vờ quan sát được màn hình, và `admitAction` từ chối hành động khi chưa quan sát được ([control-sessions.spec.ts](../packages/core/test/control-sessions.spec.ts), [computer-session.spec.ts](../apps/web/e2e/computer-session.spec.ts)). ****Nguồn preview nay đã có thật**: `BrowserDriver.capturePreview()` chụp frame thành **byte thật** (tách khỏi `observe` đúng như comment của chính nó: raw frame không bao giờ vào event log, nên observation mang reference còn byte chỉ được lấy khi có người đang nhìn màn hình), và [session-preview.ts](../apps/runtime/src/session-preview.ts) lưu byte đó vào blob store với hai điều được giữ: **byte quyết định loại** chứ không phải lời khai — một capture trả về JSON hay trang lỗi bị từ chối bằng `NOT_AN_IMAGE` thay vì được lưu và render như thể là màn hình — và **viewport đi cùng byte**, vì preview không có kích thước lúc chụp thì không thể được bày ra một cách trung thực. Test: [session-preview.spec.ts](../apps/runtime/test/session-preview.spec.ts), 5 test. **Card nay mang và render frame đó**: `browserSessionCardSchema` có `previewFrame` (digest + viewport + capturedAt) **tách tên khỏi `preview`** — vì `preview` trên card vốn đã là **trạng thái** node báo (có quan sát được surface hay không) và client đọc nó như vậy; một trường hai nghĩa là cách một card vừa nói "available" vừa không cho thấy gì. Node phục vụ byte qua `GET /previews/<digest>` (đặt **sau** chỗ kiểm bearer, vì route peer-artifact phía trước là đường không xác thực; `cache-control: no-store` vì frame là ảnh của màn hình người ta, và digest được phân giải qua guard của blob store chứ không tự nối vào path), và client render trong `ControlSessionCardBlock` qua hook đặt trong **component riêng** (card có early return, nên hook trong đó sẽ là hook chạy có điều kiện). Ảnh luôn có caption nêu thời điểm chụp — frame **không bao giờ** được trình bày như màn hình trực tiếp. **Chưa được chứng minh**: capture thật từ driver chỉ chạy trong test của driver, đường node dùng một PNG cố định của fixture — fixture chứng minh dây nối chứ không chứng minh browser render. Ghi chú điều tra: đổi mặc định của surface browser sang `unavailable` là **sai** và đã được hoàn nguyên — `preview` nghĩa là "node quan sát được bề mặt này", không phải "người dùng nhìn thấy nó", và vì `admitAction` từ chối mọi hành động khi preview không `available`, thay đổi đó sẽ làm mọi phiên browser tê liệt. |
| V15 | PARTIAL | Computer Use | Permission gating, containment labelling, target validation and profile validation are implemented and tested. **Seam cho native binding nay đã có và probe trả về lý do có tên**: `probeNativeBinding` trả `requires-signed-bundle` khi không có bundle **hoặc khi chữ ký không verify** (một chữ ký không verify được coi đúng như không có bundle — cả hai đều nghĩa là node này không có gì nó chịu chạy), và `requires-container-engine` khi bundle ổn mà engine không trả lời, mang theo **detail của chính probe engine** (cả hai lần thử `docker` và `podman` được nêu tên, vì "không có engine" và "có engine nhưng không đáp" cần hai cách sửa khác nhau). Thứ tự trong `admitNativeAction` là điểm chính: một binding không load được bị từ chối **trước khi** permission được hỏi, vì báo "chưa cấp quyền" cho thứ vốn không thể chạy sẽ đẩy người dùng tới một màn hình cài đặt không giúp gì và giấu lý do thật; và `containment` đi cùng câu trả lời của seam chứ không được quyết định sau đó — đó là thuộc tính của binding đã load. Test: [native-binding.spec.ts](../packages/execution-supervisor/test/native-binding.spec.ts), 8 test. **Chưa được chứng minh:** **hai nguồn lực thiếu vẫn thiếu** — không có bundle đã ký và không có container engine, nên binding native thật chưa từng chạy; dòng này ở lại PARTIAL với đúng hai tên đó. |
| V16 | PASS | Onboarding/personalisation | Quick play works with no credentials and is labelled as sample in a host-owned card, verified in a browser. The first screen now offers what the node knows the person was doing - unfinished work, something pinned, the session itself, a directory used recently - each chip carrying why it is there, with the four written chips as the fallback when there is nothing (`GET /suggestions`, `apps/web/e2e/suggestions.spec.ts`). Needs-based setup and preference undo are built and tested: setup is planned from the needs the person states, it "plans only the steps those needs require", "covers every need the catalogue claims to serve", reports a step as blocked and names what it waits on, and counts progress against the whole catalogue rather than the steps taken so far; undo "undoes what setup wrote and leaves the user's own changes alone" and "removes a preference that did not exist rather than inventing a value" (`packages/core/test/onboarding.spec.ts`). The product side is the first run on a node that has been told nothing: it "asks for nothing it does not need, and offers setup where the answer is used", and the card leads to the tab that fixes it (`apps/web/e2e/onboarding.spec.ts`). |
| V17 | PARTIAL | Live voice | Transcript assembly, intent routing, media-focus arbitration and mute/end semantics are implemented and tested. Commands reach the same execution path a click takes: nine app intents with a confirmation for quitting (`packages/contracts/src/app-intents.ts`, `packages/core/src/app-intents.ts`), and the window intents that actually move the desktop window (`packages/conversation-client/src/Conversation.tsx`). Parity between a sentence and a click is asserted in `apps/web/e2e/voice-control.spec.ts` and `apps/web/e2e/voice-bar.spec.ts`. The transport is a node-proxied WebSocket to Gemini Live (`packages/voice-adapters/src/gemini-live.ts`, `apps/runtime/src/voice-session.ts`), not browser-direct WebRTC, and the browser holds no provider credential (`docs/research/adr-001-gemini-live-provider.md`); the live-provider checks remain opt-in and need a provider account. **Live check nay được ghi rõ là opt-in và có probe trả lý do có tên**: `probeLiveVoice` trả `requires-live-account` khi `GEMINI_API_KEY` không được đặt và vault của node không giữ credential nào cho principal — nêu **tên** thứ nó tìm và **không bao giờ** nêu giá trị, vì probe này chạy đúng ở nơi credential có thể vắng, nên nó không được là thứ đưa credential vào log; biến rỗng được coi là vắng (một biến ai đó đã xóa không phải credential), và câu trả lời "có" chỉ nói **nguồn** (`environment` hay `vault`) chứ không có trường nào để giá trị đi qua — [voice-live.spec.ts](../apps/runtime/test/voice-live.spec.ts), 5 test chạy ở mọi lần. Phần session thật với dịch vụ của provider vẫn **opt-in** và không được thử ở đây. **Chưa được chứng minh:** không có tài khoản provider, nên một session live thật chưa từng mở; dòng này ở lại PARTIAL với đúng tên đó. |
| V18 | PASS | Operations/security | Forward-only migrations (`packages/storage/src/migrate.ts`), verifiable consistent backup, restore compatibility checks, credential redaction và durable dedup đều đã có và đã test. Failure injection và soak nay **đã được xây** chứ không còn để lại cho P10: [storage.spec.ts](../packages/storage/test/storage.spec.ts) có **6 ca tiêm lỗi quanh backup** (backup bị cắt trước khi ghi manifest, manifest trỏ vào database không còn, manifest không parse được, database bị cắt giữa lúc ghi, row count lệch manifest dù byte còn nguyên, và một soak 25 vòng tạo/verify liên tiếp) cùng **một ca tiêm lỗi quanh migration** (một migration chết giữa đường thì schema ở lại đúng phiên bản đã áp dụng trọn vẹn, và bảng nó kịp tạo cũng biến mất). Viết bộ này tìm ra **hai defect thật**: `verifyBackup` ném `ENOENT` và `database disk image is malformed` thay vì báo — đúng thứ doc của chính nó cấm, vì người vận hành đang quyết định có restore hay không — và `openDatabase` rò handle khi một pragma ném giữa chừng, khiến thư mục chứa nó không xoá được trên Windows. **Chưa có:** tiêm lỗi ở mức tiến trình đang chạy (SIGKILL một node giữa lúc ghi); bộ hiện tại tiêm ở mức file và migration. |

## Summary

### Khoảng trống đã biết: performance của widget (Phase 14)

Ba mục performance của Phase 14 nằm ở đây thay vì trong bảng, vì chúng **chưa có gì để gắn vào**:

- **pointer Orb loop ngoài React state** — đã có và đã test (`apps/web/e2e/orb.spec.ts`, canvas không bị dựng
  lại khi pointer quét qua 20 lần).
- **không duplicate live subscription sau detach** — đã có và đã test từ trước
  (`core.spec.ts`: "holds the one-owner rule across a detached surface too (V20)"). Detach trình bày cùng một
  instance ở chỗ khác, không tạo instance thứ hai, và lease từ chối chủ thứ hai kèm tên surface đang giữ.
- **heavy widget lazy mount** và **offscreen suspend** — **đã có**: surface live chỉ mount khi vào viewport và
  suspend khi rời đi (`apps/web/e2e/mini-app.spec.ts`: "a pinned surface mounts near the viewport and suspends when
  it leaves"), và frame cách ly mount trong chính surface đó. Hai điều kiện từng thiếu giờ đã có: node phục vụ
  entry của một package đã cài (`GET /packages/:id/:version/files/*`, kèm CSP theo nonce cho mỗi response), và
  frame của widget thật chạy từ hội thoại (`apps/web/e2e/widget-frame.spec.ts`: "a widget package runs in a
  sandboxed frame, and its code is the package's").

Các bảng trên là nơi tra trạng thái từng yêu cầu; không scope item nào được tuyên bố
hoàn tất. Fixture chứng minh đường nối của ứng dụng, không thay thế kiểm chứng với
provider thật. Định hướng UX trong [DESIGN.md](../DESIGN.md) không tự nâng trạng thái
conformance khi chưa có bằng chứng triển khai.
