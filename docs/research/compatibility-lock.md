# P0.1 compatibility lock — Pi SDK lifecycle

Generated by `node packages/pi-adapter/src/probe-cli.ts --write`. Do not edit by hand:
re-run the probe so the recorded evidence matches the environment it was measured in.

- **Probe:** `P0.1-pi-sdk-lifecycle`
- **Ran at:** 2026-09-16T05:22:54.346Z
- **Package:** `@earendil-works/pi-coding-agent@0.85.1`
- **Environment:** Node v24.19.0 on darwin/arm64
- **Overall:** **BLOCKED**

## Steps

| Step | Status | Detail |
|---|---|---|
| `sdk-module-load` | PASS | @earendil-works/pi-coding-agent@0.85.1 imported successfully |
| `required-exports-present` | PASS | all 6 required exports are present |
| `custom-resource-loader` | PASS | DefaultResourceLoader constructed with an explicit cwd and reloaded without touching global discovery |
| `availability-report` | PASS | adapter reports the SDK available at version 0.85.1 |
| `live-session-creation` | PASS | created session 01a0a8aa-ff93-7719-b464-1a6361464ea9 |
| `subscribe-lifecycle` | PASS | distinct listeners registered: 1; after unsubscribe: 0; duplicate registration refused: true |
| `session-dispose` | PASS | session.dispose() completed without throwing |
| `live-model-completion` | BLOCKED | not attempted by the probe: a live completion would consume the operator's provider quota and requires an account this repository does not hold |
| `command-context-ctx.reload` | BLOCKED | ctx.reload() is only available inside a Pi extension command handler; this probe runs as an embedded SDK consumer, so the step cannot be exercised from here |

## Evidence per step

### `sdk-module-load` — PASS

@earendil-works/pi-coding-agent@0.85.1 imported successfully

- resolved version: 0.85.1
- export count: 151

### `required-exports-present` — PASS

all 6 required exports are present

- present: createAgentSession, SessionManager, DefaultResourceLoader, defineTool, ModelRuntime, getAgentDir

### `custom-resource-loader` — PASS

DefaultResourceLoader constructed with an explicit cwd and reloaded without touching global discovery

- loader.reload() resolved

### `availability-report` — PASS

adapter reports the SDK available at version 0.85.1

- sdkVersion=0.85.1

### `live-session-creation` — PASS

created session 01a0a8aa-ff93-7719-b464-1a6361464ea9

- has subscribe: true
- has steer: true
- has abort: true
- has dispose: true

### `subscribe-lifecycle` — PASS

distinct listeners registered: 1; after unsubscribe: 0; duplicate registration refused: true

- a leaked listener is the mechanism behind stale handlers after a reload (T25)
- adapter reports the SDK at version 0.85.1

### `session-dispose` — PASS

session.dispose() completed without throwing

- no listener was left attached

### `live-model-completion` — BLOCKED

not attempted by the probe: a live completion would consume the operator's provider quota and requires an account this repository does not hold

- unblock condition: run `pnpm probe:pi -- --live` with a configured provider credential

### `command-context-ctx.reload` — BLOCKED

ctx.reload() is only available inside a Pi extension command handler; this probe runs as an embedded SDK consumer, so the step cannot be exercised from here

- unblock condition: load an extension through DefaultResourceLoader that calls ctx.reload() from a registered command
- what the app relies on instead: DefaultResourceLoader.reload() plus agent.state.tools assignment

## What a blocked step means here

`blocked` means the step could not be exercised in this environment and a specific condition is
recorded for unblocking it. It is not a pass, and it is not a failure. Per
`docs/implementation-plan.md` §17 a blocked step must be reported as blocked rather than renamed
to supported, so this file keeps the distinction visible.

