# pnpm 12 reads only auth/registry settings from .npmrc.
# Every other setting lives in pnpm-workspace.yaml — see that file for the
# supply-chain hardening policy (minimumReleaseAge, blockExoticSubdeps, allowBuilds).
#
# Nothing project-specific belongs here. Do not add registry tokens: credentials
# come from the developer's own keychain or environment.
